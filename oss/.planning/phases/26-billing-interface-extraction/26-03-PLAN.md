---
phase: 26-billing-interface-extraction
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - scripts/check-billing-boundary.sh
autonomous: true

must_haves:
  truths:
    - "Boundary check script passes with zero violations"
    - "No stripe imports exist outside billing/stripe/ in gateway source"
    - "No STRIPE_SECRET_KEY or STRIPE_WEBHOOK_SECRET references in gateway core (lib/, runtime/, middleware/, routes/)"
    - "Dashboard has no billing-specific UI components (no pricing page, subscription management, or billing meters)"
    - "Gateway package.json does not list stripe as a dependency"
  artifacts:
    - path: "scripts/check-billing-boundary.sh"
      provides: "Automated billing boundary validation script"
      min_lines: 40
  key_links:
    - from: "scripts/check-billing-boundary.sh"
      to: "apps/gateway/src/billing/stripe/"
      via: "excludes billing/stripe/ from violation search (allowed exception)"
      pattern: "exclude.*billing/stripe"
    - from: "scripts/check-billing-boundary.sh"
      to: "connectors/stripe/"
      via: "excludes connector-stripe from violation search (user-facing connector)"
      pattern: "exclude.*connectors"
---

<objective>
Validate dashboard has no billing UI to extract (BILL-05), create the billing boundary check script, and run final validation confirming all Phase 26 success criteria are met.

Purpose: Provide an automated, repeatable boundary check that can be run in CI to prevent billing code from leaking back into the public gateway. Also formally validates BILL-05 (dashboard billing UI extraction) which research confirmed is already satisfied.

Output: `scripts/check-billing-boundary.sh`, verified clean boundary
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-billing-interface-extraction/26-RESEARCH.md
@.planning/phases/26-billing-interface-extraction/26-01-SUMMARY.md
@.planning/phases/26-billing-interface-extraction/26-02-SUMMARY.md
@apps/gateway/src/billing/provider.ts
@apps/gateway/src/billing/middleware.ts
@apps/gateway/src/billing/types.ts
@apps/gateway/src/lib/types.ts
@apps/gateway/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate dashboard has no billing UI (BILL-05)</name>
  <files></files>
  <action>
Run validation commands to confirm the dashboard has no billing-specific UI components. This is a verification-only task -- no files are created or modified.

1. Search for billing/pricing/subscription imports in dashboard:
   ```
   grep -rn "billing\|subscription\|pricing" apps/dashboard/src/ --include='*.ts' --include='*.tsx' -i
   ```
   Filter results to exclude: comments (lines starting with `//` or `*`), connector-stripe references, marketing copy (e.g., "Stripe SDK complexity" in landing page text), node_modules.

2. Verify no billing pages exist:
   ```
   find apps/dashboard/src -name '*billing*' -o -name '*pricing*' -o -name '*subscription*'
   ```
   Expected: zero results.

3. Verify sidebar has no billing navigation:
   ```
   grep -n "billing\|pricing\|subscription\|plan" apps/dashboard/src/components/sidebar.tsx
   ```
   Expected: zero relevant results (may match "plan" in unrelated context -- verify manually if matches found).

4. Verify usage page is operational analytics, not billing meters:
   ```
   grep -n "stripe\|billing\|meter\|invoice" apps/dashboard/src/app/dashboard/usage/page.tsx
   ```
   Expected: zero results. The usage page shows API request analytics, not billing-specific meters.

If ALL checks pass: BILL-05 is satisfied. The dashboard has no billing UI components to extract. Document this finding.

If ANY check finds a billing-specific component: flag it as unexpected (research said nothing exists) and include it in the summary for manual review.
  </action>
  <verify>All grep/find commands above return zero billing-specific results (excluding false positives from marketing copy and connector-stripe references).</verify>
  <done>BILL-05 validated: dashboard has zero billing-specific UI components. No pricing page, subscription management, or billing meters exist to extract.</done>
</task>

<task type="auto">
  <name>Task 2: Create boundary check script and run final validation</name>
  <files>scripts/check-billing-boundary.sh</files>
  <action>
Create `scripts/check-billing-boundary.sh` -- an executable bash script that verifies no Stripe/billing code has leaked outside the designated provider module.

The script must check these 5 boundaries:

1. **Gateway package.json** -- `stripe` must NOT appear as a dependency:
   ```bash
   grep -q '"stripe"' apps/gateway/package.json
   ```
   If found: FAIL.

2. **Gateway source Stripe imports** -- no `from 'stripe'` or `require('stripe')` outside `billing/stripe/`:
   Search `apps/gateway/src/` for stripe import patterns, excluding:
   - `billing/stripe/` directory (allowed -- the isolated provider module)
   - `__tests__/` directory (test fixtures may reference stripe connector)

3. **STRIPE_* bindings in gateway core** -- no `STRIPE_SECRET_KEY` or `STRIPE_WEBHOOK_SECRET` in:
   - `apps/gateway/src/lib/` (types, errors, envelope)
   - `apps/gateway/src/runtime/` (factory, interfaces, adapters)
   - `apps/gateway/src/middleware/` (rate limiter, api key, etc.)
   - `apps/gateway/src/routes/` (v1, admin, internal, etc.)
   Note: `apps/gateway/src/billing/stripe/` is excluded (that's where they belong).

4. **Dashboard billing references** -- no billing-specific imports in `apps/dashboard/src/`:
   Search for `stripe`, `billing`, `subscription`, `pricing` (case-insensitive).
   Exclude: comments, marketing copy ("Stripe SDK complexity"), connector-stripe references.
   This should be a WARNING level (manual review) not a hard FAIL since false positives are likely from the word "stripe" in connector context.

5. **Gateway billing/stripe/ isolation** -- verify the stripe/ directory only imports from:
   - `'stripe'` (the SDK)
   - `'../provider'` (the interface)
   - `'../types'` (pure billing types)
   - Within `./` (sibling files in stripe/)
   Any import from `'../../lib/'`, `'../../runtime/'`, `'../../middleware/'`, etc. is a FAIL.

Script structure:
- Set `#!/usr/bin/env bash` and `set -euo pipefail`
- Track VIOLATIONS counter
- Print each check with PASS/FAIL prefix
- Exit 0 if zero violations, exit 1 if any violations
- Make the script executable: `chmod +x scripts/check-billing-boundary.sh`

After creating the script, run it: `bash scripts/check-billing-boundary.sh`

The script MUST pass with zero violations. If it fails, fix the underlying issue (likely a missed cleanup from Plan 02) and re-run.

Also run the full test suite one final time:
```
pnpm typecheck --filter @feelr/gateway
pnpm test --filter @feelr/gateway
```
  </action>
  <verify>
1. `bash scripts/check-billing-boundary.sh` exits 0 with "PASS: No billing boundary violations found"
2. `pnpm typecheck --filter @feelr/gateway` passes
3. `pnpm test --filter @feelr/gateway` passes
  </verify>
  <done>
Boundary check script exists at scripts/check-billing-boundary.sh, is executable, and passes with zero violations. All gateway tests pass. Phase 26 success criteria are fully met:
1. Gateway starts with NoopBillingProvider, all tests pass
2. StripeBillingProvider isolated in billing/stripe/ with no core imports
3. stripe not in gateway package.json
4. No billing UI in dashboard (validated)
5. Boundary check confirms no leaks
  </done>
</task>

</tasks>

<verification>
- `bash scripts/check-billing-boundary.sh` exits 0
- `pnpm typecheck --filter @feelr/gateway` passes
- `pnpm test --filter @feelr/gateway` passes
- Dashboard grep confirms zero billing components
- Phase 26 success criteria 1-5 all verified
</verification>

<success_criteria>
Billing boundary check script passes. Dashboard confirmed free of billing UI. All Phase 26 success criteria are met and validated by automated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/26-billing-interface-extraction/26-03-SUMMARY.md`
</output>
