---
phase: 03-github-connector
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - connectors/github/src/__tests__/github-fetch.test.ts
  - connectors/github/src/__tests__/flatten.test.ts
  - connectors/github/src/__tests__/actions.test.ts
  - apps/gateway/src/__tests__/github-integration.test.ts
autonomous: true

must_haves:
  truths:
    - "githubFetch correctly maps all GitHub error codes (401, 403, 404, 422, 429, 5xx) to FeelrError"
    - "Flatten functions extract the correct fields from sample GitHub API responses"
    - "Action handlers validate required params, check credentials, and return correct ActionResult shape"
    - "Action handlers populate raw field with unflattened GitHub response data for ?raw=true passthrough"
    - "Gateway dispatches requests to GitHub connector actions and returns standard envelope responses"
    - "Status endpoint returns connector health with rate limit info"
  artifacts:
    - path: "connectors/github/src/__tests__/github-fetch.test.ts"
      provides: "Tests for githubFetch helper -- error mapping, pagination parsing, header handling"
      min_lines: 80
    - path: "connectors/github/src/__tests__/flatten.test.ts"
      provides: "Tests for flattenIssue, flattenPullRequest, flattenRepository"
      min_lines: 60
    - path: "connectors/github/src/__tests__/actions.test.ts"
      provides: "Tests for action handlers -- param validation, credential check, response shape, raw field"
      min_lines: 80
    - path: "apps/gateway/src/__tests__/github-integration.test.ts"
      provides: "Integration tests for GitHub connector dispatch and status endpoint"
      min_lines: 60
  key_links:
    - from: "connectors/github/src/__tests__/github-fetch.test.ts"
      to: "connectors/github/src/github-fetch.ts"
      via: "imports githubFetch and tests with mocked fetch"
      pattern: "import.*githubFetch"
    - from: "apps/gateway/src/__tests__/github-integration.test.ts"
      to: "apps/gateway/src/app.ts"
      via: "creates test app and sends requests to /v1/github/* and /status"
      pattern: "import.*app|testClient"
---

<objective>
Comprehensive tests validating the GitHub connector pipeline: shared helpers, action handlers, gateway integration, and status endpoint.

Purpose: Prove the full pipeline works from request through dispatch, credential retrieval, GitHub API call (mocked), response flattening, and standard envelope wrapping. Includes validation that the raw field is populated for ?raw=true passthrough (locked decision). This catches integration bugs before any real GitHub API calls are made.

Output: Test suite covering error mapping, flattening, action behavior (including raw field), gateway dispatch, and status endpoint.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-github-connector/03-RESEARCH.md
@.planning/phases/03-github-connector/03-01-SUMMARY.md
@.planning/phases/03-github-connector/03-02-SUMMARY.md
@.planning/phases/03-github-connector/03-03-SUMMARY.md

@connectors/github/src/github-fetch.ts
@connectors/github/src/flatten.ts
@connectors/github/src/index.ts
@apps/gateway/src/routes/status.ts
@apps/gateway/src/__tests__/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for shared helpers (githubFetch + flatten)</name>
  <files>
    connectors/github/src/__tests__/github-fetch.test.ts
    connectors/github/src/__tests__/flatten.test.ts
  </files>
  <action>
    **Important:** Check how the existing test infrastructure works in this monorepo. Look at existing test files in `apps/gateway/src/__tests__/` and `connectors/github/package.json` to determine the test runner (likely Vitest). If the connector package doesn't have a test script or vitest config, add one. The connector tests should use standard Vitest (NOT @cloudflare/vitest-pool-workers since the connector uses only Web Standard APIs and has no Cloudflare bindings).

    Add a vitest config and test script to the GitHub connector package if needed:
    - `connectors/github/vitest.config.ts` (minimal config)
    - Add `"test": "vitest run"` to package.json scripts

    **github-fetch.test.ts:**

    Test the `githubFetch` function with a mocked `fetch` function. Create a helper that returns mock `Response` objects with configurable status, headers, and body.

    Test cases:
    1. **Successful GET request:** Mock 200 response with JSON body and rate limit headers. Verify:
       - Correct URL construction with path and query params
       - Authorization, Accept, X-GitHub-Api-Version, User-Agent headers sent
       - Response data parsed correctly
       - Rate limit extracted from headers (remaining, limit, used, reset)

    2. **Pagination parsing:** Mock 200 response with Link header containing `rel="next"`. Verify:
       - `pagination.hasMore` is true
       - `pagination.nextPage` is the correct page number
       - When no Link header, `hasMore` is false

    3. **POST with body:** Mock 201 response. Verify:
       - Method is POST
       - Content-Type: application/json header sent
       - Body serialized as JSON

    4. **204 No Content (merge):** Mock 204 response. Verify:
       - Returns empty object as data (no JSON parse attempt)

    5. **Error mapping -- 401:** Mock 401 response. Verify:
       - Throws FeelrError with code `AUTH_INVALID`, hint `auth`, status 401

    6. **Error mapping -- 403 (rate limit):** Mock 403 response with `x-ratelimit-remaining: 0`. Verify:
       - Throws FeelrError with code `RATE_LIMITED`, hint `retry`, status 429

    7. **Error mapping -- 403 (permission):** Mock 403 response with `x-ratelimit-remaining: 4999`. Verify:
       - Throws FeelrError with code `FORBIDDEN`, hint `auth`, status 403
       - Detail mentions PAT scope

    8. **Error mapping -- 404:** Mock 404 response. Verify:
       - Throws FeelrError with code `NOT_FOUND`, hint `abort`, status 404
       - Detail mentions GitHub 404 ambiguity

    9. **Error mapping -- 422 (validation):** Mock 422 response with GitHub `errors` array (`[{ resource: "Issue", field: "title", code: "missing_field" }]`). Verify:
       - Throws FeelrError with code `VALIDATION_ERROR`, hint `abort`, status 400
       - Detail contains the errors array as JSON string (locked decision: forward validation errors)

    10. **Error mapping -- 429:** Mock 429 response with Retry-After header. Verify:
        - Throws FeelrError with code `RATE_LIMITED`, hint `retry`, status 429

    11. **Error mapping -- 500:** Mock 500 response. Verify:
        - Throws FeelrError with code `UPSTREAM_ERROR`, hint `retry`, status 502

    12. **Query params skip undefined/empty values:** Pass params with undefined and empty string values. Verify they are not included in the URL.

    **flatten.test.ts:**

    Test each flatten function with realistic sample GitHub API response objects.

    1. **flattenIssue:** Provide a full GitHub issue object (nested user, labels array, assignee, etc.). Verify:
       - Returns exactly 13 keys
       - `user_login` extracted from `user.login`
       - `assignee_login` extracted from `assignee.login`
       - `labels` is array of name strings (not full label objects)
       - Nullable fields (`state_reason`, `assignee_login`, `closed_at`) return null when absent

    2. **flattenPullRequest:** Provide a full GitHub PR object. Verify:
       - Returns exactly 15 keys
       - `head_ref` from `head.ref`, `head_sha` from `head.sha`, `base_ref` from `base.ref`
       - `merged` is boolean
       - `labels` is array of name strings

    3. **flattenRepository:** Provide a full GitHub repo object. Verify:
       - Returns exactly 12 keys
       - Nullable fields (`description`, `language`) handled correctly
       - Numeric fields (`stargazers_count`, `forks_count`) passed through

    4. **Edge cases:** Missing nested objects (user is null), empty labels array, all nullable fields absent.
  </action>
  <verify>
    Run tests from the connector package: `cd connectors/github && pnpm test` (or `pnpm turbo test --filter=@feelr/connector-github`). All tests must pass.
  </verify>
  <done>
    - githubFetch tests cover: successful requests, pagination parsing, POST with body, 204 handling, all error codes (401/403-rate/403-perm/404/422/429/500+)
    - Flatten tests verify field counts and correct extraction from nested GitHub response objects
    - Error mapping tests confirm validation errors are forwarded (locked decision)
    - Edge cases for missing/null nested objects covered
  </done>
</task>

<task type="auto">
  <name>Task 2: Action handler tests (with raw field) and gateway integration tests</name>
  <files>
    connectors/github/src/__tests__/actions.test.ts
    apps/gateway/src/__tests__/github-integration.test.ts
  </files>
  <action>
    **actions.test.ts (in connector package):**

    Test a representative subset of action handlers (not all 10 -- test the distinct patterns):

    1. **issues.list handler:**
       - Mock fetch to return a GitHub issues array (include one item with `pull_request` key)
       - Verify: PR item is filtered out, remaining items flattened, pagination meta returned
       - Verify: cursor passed as `page` query param to GitHub API
       - **Verify: `raw` field contains the FULL unfiltered, unflattened array from GitHub (including the PR item)** -- this is critical for ?raw=true passthrough (locked decision)

    2. **issues.get handler:**
       - Mock fetch to return a single GitHub issue object with nested fields (user, labels, assignee)
       - **Verify: `data` is the flattened issue (13 fields), `raw` is the full unflattened GitHub issue object with all nested fields intact**

    3. **issues.create handler:**
       - Mock fetch to return created issue
       - Verify: comma-separated labels split into array in request body
       - Verify: response flattened correctly
       - **Verify: `raw` field contains the full GitHub response object**

    4. **pulls.merge handler:**
       - Mock fetch to return 200 with `{ sha, merged: true, message }`
       - Verify: response shape is correct (not flattened with flattenPullRequest)
       - **Verify: `raw` field contains the full GitHub merge response**

    5. **repos.list handler:**
       - Mock fetch to return repo array
       - Verify: calls `/user/repos` (no owner/repo in path)
       - Verify: repos flattened correctly
       - **Verify: `raw` field contains unflattened repo array from GitHub**

    6. **Credential check:**
       - Call any action handler without credential
       - Verify: throws FeelrError AUTH_REQUIRED / 401 / hint: auth

    7. **Repo param validation:**
       - Call issues.list with invalid repo format (no slash)
       - Verify: throws FeelrError VALIDATION_ERROR / 400 / hint: abort

    8. **Raw field contract (general):**
       - For any action, verify that `result.raw` is defined (not undefined)
       - Verify that `result.raw` is NOT the same object reference as `result.data` (raw is unflattened, data is flattened)
       - For list actions, verify `raw` is the original array type, `data` is an array of flattened objects

    **github-integration.test.ts (in gateway):**

    Integration tests using the gateway app with mocked external fetch. These tests use @cloudflare/vitest-pool-workers (same setup as existing gateway tests). Check existing test files for the pattern.

    Test approach: The GitHub connector's action handlers call `ctx.fetch` which in the gateway dispatch is `fetch` (global). We need to mock the global fetch or intercept outbound requests. Check how the existing gateway tests handle this -- they may use miniflare's fetch mock or a custom approach.

    If mocking is complex, focus on testing that:

    1. **Connector routing works:**
       - `GET /v1/github/issues.list?repo=owner/repo&key=fk_test_xxx_yyy` routes to the GitHub connector
       - Requires a valid API key in KV (set up in test fixtures)
       - The response has the standard envelope shape: `{ ok, data, meta }`

    2. **Unknown action returns 404:**
       - `GET /v1/github/nonexistent?key=fk_test_xxx_yyy` returns `{ ok: false, error: { code: 'ACTION_NOT_FOUND' } }`

    3. **Status endpoint -- no credentials stored:**
       - `GET /status` with valid API key but no GitHub credentials in KV
       - Returns `{ ok: true, data: { gateway: { status: 'healthy' }, connectors: {} } }`

    4. **Status endpoint -- no API key:**
       - `GET /status` without API key returns 401

    **Note on fetch mocking:** If the test environment makes mocking external fetch difficult (the Cloudflare Workers test runtime may not support standard fetch mocking), adjust the test approach:
    - For action handler tests in the connector package (standard Vitest), mock `fetch` by passing a mock function as `ctx.fetch`
    - For gateway integration tests, test routing and error responses (which don't require external fetch) rather than full end-to-end with mocked GitHub responses

    The key insight: action handler unit tests (in connector package, standard Vitest, mock ctx.fetch) test the logic. Gateway integration tests (pool-workers, real KV) test the routing and envelope wrapping.
  </action>
  <verify>
    Run all tests: `pnpm turbo test` from the monorepo root. All tests must pass including both connector-level and gateway-level tests. No regressions in existing Phase 1/2 tests.
  </verify>
  <done>
    - Action handler tests verify: PR filtering in issues.list, comma-split labels, merge response shape, repos.list path, credential check, repo param validation
    - Action handler tests verify: raw field populated with unflattened GitHub data on every action (locked decision: ?raw=true passthrough)
    - Raw field contract verified: raw is defined, raw differs from data (unflattened vs flattened), raw preserves original GitHub response structure
    - Gateway integration tests verify: connector routing, unknown action 404, status endpoint behavior, API key requirement on /status
    - All existing tests still pass (no regressions)
    - Full test coverage of the GitHub connector pipeline from dispatch to response
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo test` passes across all packages (connector + gateway)
- No regressions in Phase 1/2 tests
- Error mapping tests confirm all GitHub error codes handled
- Flatten tests confirm correct field counts (13, 15, 12)
- Action handler tests confirm raw field is populated with unflattened data for all tested actions
- Integration tests confirm routing, envelope wrapping, and status endpoint
</verification>

<success_criteria>
- githubFetch error mapping tested for all status codes (401, 403-rate, 403-perm, 404, 422, 429, 5xx)
- Flatten functions tested with realistic GitHub API response samples
- Representative action handlers tested (list with PR filter, create with comma-split, merge response, repos.list path)
- Raw field populated on all action results -- verified by tests (locked decision: ?raw=true passthrough)
- Gateway dispatch routes to GitHub connector correctly
- Status endpoint requires API key and returns correct shape
- All tests green, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-github-connector/03-04-SUMMARY.md`
</output>
