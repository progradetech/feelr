---
phase: 06-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/dashboard/src/app/(dashboard)/keys/page.tsx
  - apps/dashboard/src/components/key-create-dialog.tsx
  - apps/dashboard/src/components/key-reveal.tsx
  - apps/dashboard/src/components/key-revoke-dialog.tsx
  - apps/dashboard/src/lib/hooks/use-keys.ts
autonomous: true

must_haves:
  truths:
    - "User can create a new API key via a modal dialog with optional label"
    - "Newly created key is shown once in a highlighted box with copy button -- full key visible only until dismissed"
    - "User can view all API keys in a list showing label, short token (masked), created date, and last used date"
    - "User can revoke a key only after typing its name/label to confirm (GitHub repo deletion pattern)"
  artifacts:
    - path: "apps/dashboard/src/app/(dashboard)/keys/page.tsx"
      provides: "API key management page with list and actions"
      min_lines: 40
    - path: "apps/dashboard/src/components/key-create-dialog.tsx"
      provides: "Modal dialog for creating new API keys"
      min_lines: 30
    - path: "apps/dashboard/src/components/key-reveal.tsx"
      provides: "One-time key reveal component with copy button"
      min_lines: 25
    - path: "apps/dashboard/src/components/key-revoke-dialog.tsx"
      provides: "Type-to-confirm revocation dialog"
      min_lines: 30
    - path: "apps/dashboard/src/lib/hooks/use-keys.ts"
      provides: "SWR hook for API key data fetching"
      exports: ["useKeys"]
  key_links:
    - from: "apps/dashboard/src/lib/hooks/use-keys.ts"
      to: "gateway /admin/keys"
      via: "gatewayFetch GET"
      pattern: "gatewayFetch.*admin/keys"
    - from: "apps/dashboard/src/components/key-create-dialog.tsx"
      to: "gateway POST /admin/keys"
      via: "gatewayMutate POST"
      pattern: "gatewayMutate.*admin/keys.*POST"
    - from: "apps/dashboard/src/components/key-revoke-dialog.tsx"
      to: "gateway DELETE /admin/keys/:id"
      via: "gatewayMutate DELETE"
      pattern: "gatewayMutate.*admin/keys.*DELETE"
---

<objective>
Build the API Keys management page with create (modal + one-time reveal), list, and revoke (type-to-confirm) functionality.

Purpose: DASH-01 requires users to create, view, and revoke API keys from the dashboard. This page is the most interaction-heavy page with locked UX patterns (one-time reveal, type-to-confirm revocation).

Output: Complete keys page with SWR data fetching, creation dialog, one-time key reveal, and type-to-confirm revocation.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-RESEARCH.md
@.planning/phases/06-dashboard/06-02-SUMMARY.md
@apps/dashboard/src/lib/api.ts
@apps/dashboard/src/lib/types.ts
@apps/gateway/src/routes/keys.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SWR hook + Keys list page with detailed info density</name>
  <files>
    apps/dashboard/src/lib/hooks/use-keys.ts
    apps/dashboard/src/app/(dashboard)/keys/page.tsx
  </files>
  <action>
    1. Create `apps/dashboard/src/lib/hooks/use-keys.ts`:
    - 'use client' at top.
    - Import `useSWR` and `gatewayFetch` and `ApiKey` type.
    - Export `useKeys()` hook: `return useSWR('admin-keys', () => gatewayFetch<ApiKey[]>('/admin/keys'))`.
    - The hook returns `{ data, error, isLoading, mutate }` from SWR.

    2. Replace the stub `apps/dashboard/src/app/(dashboard)/keys/page.tsx` with the full keys page:
    - 'use client' page.
    - Page header: "API Keys" title + "Create Key" button (top right) that opens the create dialog.
    - Key list as a table or card list (per discretion: use a clean table with columns):
      - Label (or "Unnamed" if null, in zinc-400 italic)
      - Short Token: show `fk_..._${short_token}` in monospace, truncated
      - Created: formatted date via date-fns `formatDistanceToNow` (e.g., "3 days ago")
      - Last Used: formatted date or "Never" in zinc-500
      - Actions: "Revoke" button (destructive variant, small)
    - Loading state: show skeleton cards or "Loading..." text.
    - Empty state: "No API keys yet. Create one to get started." with prominent create button.
    - The page manages two pieces of state:
      a. `showCreateDialog: boolean` -- controls create dialog visibility.
      b. `revokeTarget: ApiKey | null` -- controls revoke dialog visibility and which key is being revoked.
      c. `revealedKey: string | null` -- the full key string returned after creation (shown in KeyReveal component).
    - When create succeeds: set `revealedKey` to the returned full key, call `mutate()` to refresh the list.
    - When revoke succeeds: set `revokeTarget` to null, call `mutate()` to refresh the list, show success toast.
  </action>
  <verify>
    Run `cd /home/eternaldays/claudeRepos/feelr/apps/dashboard && pnpm next build` -- build succeeds.
    Verify keys page exists at `out/keys.html` in the build output.
    Grep for `useKeys` in keys/page.tsx and hooks/use-keys.ts.
  </verify>
  <done>
    Keys page shows a detailed list of API keys with metadata, loading/empty states, and triggers for create and revoke dialogs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Key creation dialog, one-time reveal, and type-to-confirm revocation</name>
  <files>
    apps/dashboard/src/components/key-create-dialog.tsx
    apps/dashboard/src/components/key-reveal.tsx
    apps/dashboard/src/components/key-revoke-dialog.tsx
  </files>
  <action>
    1. Create `apps/dashboard/src/components/key-create-dialog.tsx`:
    - 'use client' component.
    - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `onCreated: (fullKey: string) => void`.
    - Uses shadcn Dialog with DialogContent, DialogHeader, DialogTitle.
    - Form with a single "Label" input (optional, placeholder "e.g., production-agent").
    - "Create Key" submit button. On submit:
      a. Call `gatewayMutate<{ key: string; short_token: string; label: string | null; created_at: string }>('/admin/keys', 'POST', { label: label || undefined })`.
      b. On success: call `onCreated(response.key)`, close dialog.
      c. On error: show error toast via sonner.
    - Loading state on button while creating.

    2. Create `apps/dashboard/src/components/key-reveal.tsx`:
    - 'use client' component.
    - Props: `fullKey: string`, `onDismiss: () => void`.
    - Amber/warning styled box (per CONTEXT.md locked decision -- GitHub PAT pattern):
      - Warning text: "Make sure to copy your API key now. You won't be able to see it again."
      - Full key displayed in monospace code block with green text on dark background.
      - "Copy" button using `navigator.clipboard.writeText(fullKey)`. Shows "Copied!" for 2 seconds after click. Success toast via sonner.
      - "I've copied my key" dismissal button that calls `onDismiss`.
    - This component appears at the top of the keys page after creation, pushing the list down.

    3. Create `apps/dashboard/src/components/key-revoke-dialog.tsx`:
    - 'use client' component.
    - Props: `keyLabel: string`, `shortToken: string`, `open: boolean`, `onOpenChange: (open: boolean) => void`, `onConfirm: () => void`.
    - Uses shadcn Dialog.
    - Destructive warning text: "This action cannot be undone. This will permanently revoke the API key."
    - Confirmation pattern (per CONTEXT.md locked decision -- GitHub repo deletion pattern):
      - Display the confirmation target: use `keyLabel` if it exists, otherwise `shortToken`.
      - Text: "To confirm, type **{confirmText}** below."
      - Input field. The "Revoke Key" button (destructive variant) is DISABLED until input matches confirmText exactly.
    - On confirm click:
      a. Call `gatewayMutate('/admin/keys/${shortToken}', 'DELETE')`.
      b. On success: call `onConfirm()`.
      c. On error: show error toast.
    - Loading state on button while revoking.
    - Reset input text when dialog closes.
  </action>
  <verify>
    Run `cd /home/eternaldays/claudeRepos/feelr/apps/dashboard && pnpm next build` -- build succeeds.
    Grep for `key-create-dialog` import in keys/page.tsx.
    Grep for `key-reveal` import in keys/page.tsx.
    Grep for `key-revoke-dialog` import in keys/page.tsx.
    Grep for `navigator.clipboard` in key-reveal.tsx.
    Grep for `confirmation.*===.*confirmText` or equivalent match check in key-revoke-dialog.tsx.
  </verify>
  <done>
    Key creation dialog creates keys via gateway API. One-time reveal shows the full key with copy button and "won't see again" warning. Revocation requires typing the key name to confirm. All per locked decisions.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/dashboard && pnpm next build` succeeds.
2. Keys page lists keys with label, short token, created date, last used date.
3. Create dialog calls POST /admin/keys and returns the full key.
4. Key reveal shows full key once with copy button and amber warning box.
5. Revoke dialog requires typing the key name/label to enable the revoke button.
6. SWR hook refreshes the list after create and revoke operations.
</verification>

<success_criteria>
- User can create an API key via modal dialog with optional label
- Full key is revealed once with copy button in highlighted amber box
- Key list shows label, masked short token, created date, last used date
- Revocation requires typing key name to confirm (destructive safety pattern)
- All CRUD operations go through gateway /admin/keys endpoints
- Loading and error states handled throughout
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard/06-03-SUMMARY.md`
</output>
