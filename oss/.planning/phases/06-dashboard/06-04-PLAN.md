---
phase: 06-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/dashboard/src/app/(dashboard)/connectors/page.tsx
  - apps/dashboard/src/components/connector-card.tsx
  - apps/dashboard/src/lib/hooks/use-connectors.ts
  - apps/dashboard/src/app/(dashboard)/overview/page.tsx
  - apps/dashboard/src/lib/hooks/use-overview.ts
autonomous: true

must_haves:
  truths:
    - "Connected services view shows all 4 connectors (GitHub, Slack, Stripe, Discord) with auth status as cards"
    - "Each connector card shows one of 3 status states: connected (green), needs re-auth (amber), not connected (gray)"
    - "Each connector card shows a CLI command to reconnect (e.g., feelr auth slack)"
    - "Overview page shows summary cards: total keys, connected services count, and recent usage sparkline"
  artifacts:
    - path: "apps/dashboard/src/app/(dashboard)/connectors/page.tsx"
      provides: "Connector status page with card grid"
      min_lines: 30
    - path: "apps/dashboard/src/components/connector-card.tsx"
      provides: "Individual connector status card"
      min_lines: 25
    - path: "apps/dashboard/src/app/(dashboard)/overview/page.tsx"
      provides: "Overview landing page with summary cards"
      min_lines: 40
    - path: "apps/dashboard/src/lib/hooks/use-overview.ts"
      provides: "SWR hook for overview data"
      exports: ["useOverview"]
  key_links:
    - from: "apps/dashboard/src/lib/hooks/use-connectors.ts"
      to: "gateway /admin/credentials + /status"
      via: "gatewayFetch for connector status"
      pattern: "gatewayFetch.*(admin/credentials|status)"
    - from: "apps/dashboard/src/lib/hooks/use-overview.ts"
      to: "gateway /internal/overview"
      via: "gatewayFetch for summary data"
      pattern: "gatewayFetch.*internal/overview"
    - from: "apps/dashboard/src/app/(dashboard)/overview/page.tsx"
      to: "recharts"
      via: "Sparkline area chart for recent usage"
      pattern: "AreaChart|ResponsiveContainer"
---

<objective>
Build the Connectors status page with card grid and the Overview landing page with summary cards and sparkline.

Purpose: DASH-02 requires connected services view with auth status. The Overview page is the dashboard landing page per locked decision (sidebar Overview section). Both pages are relatively simpler than keys or usage and group naturally.

Output: Connector cards showing 3 status states with CLI reconnect instructions, and Overview page with summary cards + sparkline chart.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard/06-CONTEXT.md
@.planning/phases/06-dashboard/06-RESEARCH.md
@.planning/phases/06-dashboard/06-01-SUMMARY.md
@.planning/phases/06-dashboard/06-02-SUMMARY.md
@apps/dashboard/src/lib/api.ts
@apps/dashboard/src/lib/types.ts
@apps/gateway/src/routes/status.ts
@apps/gateway/src/routes/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connectors page with status cards and re-auth CLI instructions</name>
  <files>
    apps/dashboard/src/lib/hooks/use-connectors.ts
    apps/dashboard/src/components/connector-card.tsx
    apps/dashboard/src/app/(dashboard)/connectors/page.tsx
  </files>
  <action>
    1. Create `apps/dashboard/src/lib/hooks/use-connectors.ts`:
    - 'use client'.
    - Export `useConnectors()` hook.
    - The gateway has two relevant endpoints: GET /admin/credentials (lists connected connectors) and GET /status (lists all registered connectors with auth_state).
    - The /status endpoint requires an API key (not admin token), which the dashboard does NOT have. So instead, use a different approach:
      a. Fetch GET /admin/credentials to get the list of stored connectors (returns `[{connector: "github"}, ...]`).
      b. Define a constant list of ALL known connectors: `['github', 'slack', 'stripe', 'discord']`.
      c. Map each known connector to its status: if it appears in credentials list -> 'connected', otherwise -> 'not_connected'.
      d. NOTE: 'needs_reauth' state cannot be determined from /admin/credentials alone. For Phase 6, use 2 states (connected / not_connected). The 3-state requirement (connected / needs re-auth / not connected) will need the gateway to expose auth_state on the /admin/credentials response. Add a TODO comment noting this limitation.
    - Actually, a better approach: add a simple check. The gateway /status endpoint is API-key-authenticated, but we could add a new internal route. Instead, keep it simple for Phase 6: the hook fetches /admin/credentials, and for the 'needs_reauth' state, check if the credential is known to have an expiration issue. Since we can't determine this from current endpoints, map: credentials list present -> 'connected', not present -> 'not_connected'. Leave needs_reauth as a TODO for when the internal routes expose richer credential metadata.
    - Return type: `ConnectorStatus[]` where each has `{ name, status, auth_command }`.

    2. Create `apps/dashboard/src/components/connector-card.tsx`:
    - 'use client' component.
    - Props: `name: string`, `status: 'connected' | 'needs_reauth' | 'not_connected'`, `authCommand: string`.
    - Card (shadcn Card component) with:
      - Connector name as title (capitalize first letter: "GitHub", "Slack", "Stripe", "Discord").
      - Status badge (shadcn Badge):
        - 'connected': green badge text "Connected", with a green dot indicator.
        - 'needs_reauth': amber badge text "Needs Re-auth", with amber dot indicator.
        - 'not_connected': gray badge text "Not Connected", with gray dot indicator.
      - Icon placeholder area (use lucide-react icons: Github for github, MessageSquare for slack, CreditCard for stripe, Gamepad2 for discord. Or use a simple colored circle if exact icons aren't available).
      - CLI reconnect instructions: show `authCommand` in a monospace code block at the bottom of the card (e.g., `feelr auth github`). For 'connected' status, show as muted helper text. For 'not_connected' or 'needs_reauth', show as a prominent call-to-action.
    - Card has subtle hover effect (zinc-800/50 background on hover).

    3. Replace the stub `apps/dashboard/src/app/(dashboard)/connectors/page.tsx`:
    - 'use client' page.
    - Header: "Connected Services" title.
    - 4-column grid on large screens (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4`).
    - Map each connector from `useConnectors()` to a ConnectorCard.
    - Loading state: 4 skeleton cards.
    - Map connector names to auth commands: github -> `feelr auth github`, slack -> `feelr auth slack`, etc.
  </action>
  <verify>
    Run `cd /home/eternaldays/claudeRepos/feelr/apps/dashboard && pnpm next build` -- build succeeds.
    Verify connectors page exists in build output.
    Grep for `ConnectorCard` in connectors/page.tsx.
    Grep for `feelr auth` in connector-card.tsx to confirm CLI instruction.
  </verify>
  <done>
    Connectors page shows a card grid of all 4 connectors with status badges (connected/not_connected) and CLI reconnect commands per locked decision.
  </done>
</task>

<task type="auto">
  <name>Task 2: Overview landing page with summary cards and sparkline</name>
  <files>
    apps/dashboard/src/lib/hooks/use-overview.ts
    apps/dashboard/src/app/(dashboard)/overview/page.tsx
  </files>
  <action>
    1. Create `apps/dashboard/src/lib/hooks/use-overview.ts`:
    - 'use client'.
    - Export `useOverview()` hook.
    - Uses SWR: `return useSWR('internal-overview', () => gatewayFetch<OverviewData>('/internal/overview'))`.
    - The OverviewData type is already defined in types.ts.

    2. Replace the stub `apps/dashboard/src/app/(dashboard)/overview/page.tsx`:
    - 'use client' page.
    - Header: "Overview" title with a welcome message or subtitle.

    3. Summary cards section (3 cards in a row, using shadcn Card):
    - Card 1: "Total API Keys" -- large number from `data.total_keys`. Icon: Key from lucide-react.
    - Card 2: "Connected Services" -- number from `data.connected_services`, with total possible (e.g., "2 / 4"). Icon: Plug from lucide-react.
    - Card 3: "Requests (24h)" -- number from `data.recent_usage.total_24h`. Icon: Activity from lucide-react.
    - Cards have zinc-900 background, zinc-400 label text, white number text.

    4. Sparkline section below cards:
    - Title: "Recent Activity" with subtitle "Last 24 hours".
    - Use Recharts AreaChart (ResponsiveContainer, Area, XAxis, Tooltip).
    - Data from `data.recent_usage.hourly` -- array of `{hour, count}`.
    - Area chart with blue fill (#3b82f6 stroke, #3b82f680 fill).
    - XAxis shows abbreviated hour (format the hour string to show just HH:MM).
    - Height: 200px.
    - Handle empty data: show "No usage data yet" message.

    5. Loading state: skeleton cards and empty chart area.
    6. Error state: show error message with retry button.
  </action>
  <verify>
    Run `cd /home/eternaldays/claudeRepos/feelr/apps/dashboard && pnpm next build` -- build succeeds.
    Verify overview page exists in build output.
    Grep for `useOverview` in overview/page.tsx.
    Grep for `AreaChart` in overview/page.tsx to confirm sparkline.
    Grep for `total_keys` or `total_api_keys` in overview/page.tsx.
  </verify>
  <done>
    Overview page shows 3 summary cards (total keys, connected services, 24h requests) and a sparkline area chart of recent hourly usage per locked decision.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/dashboard && pnpm next build` succeeds.
2. Connectors page renders 4 connector cards with status badges.
3. Each connector card shows CLI auth command.
4. Overview page shows 3 summary cards with data from /internal/overview.
5. Sparkline chart renders hourly usage data from Recharts AreaChart.
6. Both pages handle loading, empty, and error states.
</verification>

<success_criteria>
- Connector cards show GitHub, Slack, Stripe, Discord with connected/not_connected status
- Status badges use appropriate colors (green for connected, gray for not connected)
- CLI reconnect instruction visible on each connector card
- Overview shows total keys, connected services count, 24h request count
- Sparkline area chart displays recent hourly usage data
- All data fetched from gateway API routes (no direct KV/D1 access)
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard/06-04-SUMMARY.md`
</output>
