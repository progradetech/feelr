---
phase: 17-cicd-homebrew-migration
plan: 03
type: execute
wave: 1
depends_on: ["17-02"]
files_modified: []
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Running brew tap andrewprograde/feelr finds the deprecation formula"
    - "Deprecation formula is at Formula/feelr.rb in andrewprograde/homebrew-feelr (not root)"
  artifacts:
    - path: "andrewprograde/homebrew-feelr/Formula/feelr.rb"
      provides: "Deprecation formula at correct Homebrew tap path"
  key_links:
    - from: "andrewprograde/homebrew-feelr/Formula/feelr.rb"
      to: "brew tap andrewprograde/feelr"
      via: "Homebrew tap directory convention"
      pattern: "Formula/feelr.rb"
---

<objective>
Fix the deprecation formula path in andrewprograde/homebrew-feelr so Homebrew can discover it.

Purpose: The deprecation formula was placed at `feelr.rb` (root) instead of `Formula/feelr.rb`. Homebrew taps require formulas in the `Formula/` directory. Without this fix, `brew tap andrewprograde/feelr` will not find the formula, and users won't see the deprecation message directing them to `progradetech/feelr`.

Output: Deprecation formula correctly located at `Formula/feelr.rb` in the andrewprograde/homebrew-feelr repository.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-cicd-homebrew-migration/17-02-SUMMARY.md
@.planning/phases/17-cicd-homebrew-migration/17-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move deprecation formula to Formula/ directory in andrewprograde/homebrew-feelr</name>
  <files>andrewprograde/homebrew-feelr/Formula/feelr.rb (external repo)</files>
  <action>
    The deprecation formula currently sits at the root of the andrewprograde/homebrew-feelr repo (`feelr.rb`). Homebrew requires formulas in the `Formula/` directory.

    Steps:
    1. Clone andrewprograde/homebrew-feelr: `git clone git@github.com:andrewprograde/homebrew-feelr.git /tmp/andrewprograde-homebrew-feelr`
    2. Inside the clone, create the Formula directory: `mkdir -p Formula`
    3. Move the formula: `git mv feelr.rb Formula/feelr.rb`
    4. Commit: `git commit -m "fix: move deprecation formula to Formula/ directory for Homebrew tap discovery"`
    5. Push: `git push origin main`
    6. Clean up: `rm -rf /tmp/andrewprograde-homebrew-feelr`

    Do NOT modify the formula contents -- only move the file. The formula already has the correct `deprecate!` directive, `odie` install block, and `caveats` migration instructions from Plan 17-02.
  </action>
  <verify>
    Verify the file exists at the correct path in the remote repo:
    ```bash
    gh api repos/andrewprograde/homebrew-feelr/contents/Formula/feelr.rb --jq '.name'
    ```
    Expected output: `feelr.rb`

    Verify the old root-level file is gone:
    ```bash
    gh api repos/andrewprograde/homebrew-feelr/contents/feelr.rb 2>&1 | grep -c "Not Found"
    ```
    Expected output: `1`
  </verify>
  <done>Formula/feelr.rb exists in andrewprograde/homebrew-feelr and root-level feelr.rb is removed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Homebrew tap discovers the deprecation formula</name>
  <files></files>
  <action>
Present verification steps to the user. Claude has completed all automatable work: deprecation formula moved to Formula/feelr.rb. Now the user verifies Homebrew tap discovery works correctly.
  </action>
  <verify>
**Step 1: Untap if already tapped**
```bash
brew untap andrewprograde/feelr 2>/dev/null; true
```

**Step 2: Tap and check formula discovery**
```bash
brew tap andrewprograde/feelr
brew info andrewprograde/feelr/feelr
```

**Expected results:**
- `brew tap` succeeds without errors
- `brew info` shows the formula with deprecation status
- Caveats section shows migration instructions to `brew install progradetech/feelr/feelr`

**Alternative (if Homebrew not available):**
```bash
gh api repos/andrewprograde/homebrew-feelr/contents/Formula/feelr.rb --jq '.content' | base64 -d | head -20
```
Confirm the file contains `deprecate!` and `progradetech/feelr/feelr` in caveats.
  </verify>
  <done>User confirms `brew tap andrewprograde/feelr` discovers the formula and shows deprecation, or programmatic API check confirms formula content at Formula/feelr.rb</done>
</task>

</tasks>

<verification>
## Gap Closure Verification

| Gap | Status | Verification |
|-----|--------|-------------|
| Gap 1: Formula wrong path (BLOCKER) | FIXED | `Formula/feelr.rb` exists in andrewprograde/homebrew-feelr |
| Gap 2: CF token unverified (DEFERRED) | DEFERRED | Low risk -- user confirmed dashboard change; will verify on next gateway deploy |
| Gap 3: New tap empty (EXPECTED) | NO ACTION | Expected state; GoReleaser publishes on next release (v1.2.0+) |

**Gap 2 rationale for deferral:** The Cloudflare API token was updated by the user in the Cloudflare dashboard (confirmed during Plan 01 Task 2 checkpoint). There is no gateway code change in this phase to trigger the workflow. Forcing a trivial change just to verify adds noise. The token will be implicitly verified on the next natural gateway deployment. This is documented as a known deferred item in STATE.md.

**Gap 3 rationale for no action:** The progradetech/homebrew-feelr repo is empty because the GoReleaser config change (commit fb6426c) happened AFTER the v1.1.0 release. The new tap will receive its first formula when v1.2.0 (or later) is tagged. This is the expected state, not a bug.
</verification>

<success_criteria>
1. `gh api repos/andrewprograde/homebrew-feelr/contents/Formula/feelr.rb` returns 200
2. `gh api repos/andrewprograde/homebrew-feelr/contents/feelr.rb` returns 404
3. Human confirms `brew tap andrewprograde/feelr` discovers the formula (or programmatic API check passes)
</success_criteria>

<output>
After completion, create `.planning/phases/17-cicd-homebrew-migration/17-03-SUMMARY.md`
</output>
