---
phase: 31-cloud-deploy-workflows
plan: 03
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - .github/workflows/docs.yml
autonomous: true
user_setup:
  - service: azure-swa
    why: "Docs site deploys to Azure Static Web Apps"
    env_vars:
      - name: SWA_DOCS_STAGING_TOKEN
        source: "Azure Portal -> Static Web Apps -> feelr-docs-staging -> Manage deployment token"
      - name: SWA_DOCS_DEPLOYMENT_TOKEN
        source: "Azure Portal -> Static Web Apps -> feelr-docs -> Manage deployment token"

must_haves:
  truths:
    - "Turbo build for @feelr/docs produces output in oss/apps/docs/out/"
    - "Docs deploy workflow builds and uploads static export to Azure SWA"
    - "Staging deploy sets NEXT_PUBLIC_SITE_URL to staging-docs.feelr.dev"
    - "Production deploy sets NEXT_PUBLIC_SITE_URL to feelr.dev"
  artifacts:
    - path: ".github/workflows/docs.yml"
      provides: "Docs deploy workflow (staging + production)"
      contains: "SWA_DOCS"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "turbo.json"
      via: "pnpm turbo run build"
      pattern: "turbo run build"
    - from: ".github/workflows/docs.yml"
      to: "oss/apps/docs/out"
      via: "SWA upload app_location"
      pattern: "oss/apps/docs/out"
---

<objective>
Verify and fix the cloud docs deploy workflow so it builds and deploys to Azure Static Web Apps.

Purpose: The docs site is the developer-facing documentation. It uses the same turbo + SWA pattern as the dashboard. With turbo.json fixed in Plan 31-02, the docs build should work. This plan validates the docs-specific workflow configuration.

Output: A verified docs.yml workflow ready for deployment.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-cloud-deploy-workflows/31-02-SUMMARY.md
@.github/workflows/docs.yml
@turbo.json
@oss/apps/docs/next.config.mjs
@oss/apps/docs/package.json
@oss/apps/docs/staticwebapp.config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify docs build and review workflow configuration</name>
  <files>.github/workflows/docs.yml</files>
  <action>
1. Verify the docs site builds successfully with turbo (turbo.json was fixed in Plan 31-02):
   ```
   NEXT_PUBLIC_SITE_URL=https://staging-docs.feelr.dev \
   pnpm turbo run build --filter=@feelr/docs
   ```
   This should produce output in `oss/apps/docs/out/`.

2. Verify `oss/apps/docs/staticwebapp.config.json` exists (needed by the "Copy SWA config" step).

3. Verify `oss/apps/docs/out/health/index.html` or equivalent exists after build (for smoke test at /health).

4. Review `.github/workflows/docs.yml` for correctness:
   - Staging job: NEXT_PUBLIC_SITE_URL=https://staging-docs.feelr.dev -- correct
   - Production job: NEXT_PUBLIC_SITE_URL=https://feelr.dev -- correct
   - SWA deploy uses `app_location: oss/apps/docs/out` and `skip_app_build: true` -- correct
   - Staging uses `SWA_DOCS_STAGING_TOKEN`, production uses `SWA_DOCS_DEPLOYMENT_TOKEN` -- correct
   - No CF analytics or gateway URL needed (docs is pure content, per workflow comment)

5. If the workflow file has any issues (wrong paths, missing steps, incorrect env vars), fix them.

6. Clean up build output: `rm -rf oss/apps/docs/out oss/apps/docs/.next`
  </action>
  <verify>
- Docs build completes without errors: `oss/apps/docs/out/index.html` exists after turbo build
- `oss/apps/docs/out/health.html` or `oss/apps/docs/out/health/index.html` exists (for smoke test)
- `.github/workflows/docs.yml` references correct SWA tokens and env vars
- Build output is cleaned up (not committed)
  </verify>
  <done>
Docs site builds successfully with turbo, producing static export in out/. The health page renders for smoke tests. The workflow is correctly configured with proper NEXT_PUBLIC_SITE_URL values, SWA tokens, and app_location path. SWA deployment tokens are documented as user_setup requirements.
  </done>
</task>

</tasks>

<verification>
1. Docs builds with turbo: `pnpm turbo run build --filter=@feelr/docs` succeeds
2. Build output contains health page for smoke test
3. `.github/workflows/docs.yml` has correct NEXT_PUBLIC_SITE_URL for staging and production
4. `.github/workflows/docs.yml` uses correct SWA secret names
5. No unnecessary env vars (no gateway URL or CF analytics for docs)
</verification>

<success_criteria>
The docs site builds successfully with turbo (using the turbo.json fixed in Plan 31-02) and the workflow is correctly configured for staging and production deploys to Azure SWA.
</success_criteria>

<output>
After completion, create `.planning/phases/31-cloud-deploy-workflows/31-03-SUMMARY.md`
</output>
