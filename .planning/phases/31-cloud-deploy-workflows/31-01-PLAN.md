---
phase: 31-cloud-deploy-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/gateway.yml
  - cloud/gateway/package.json
autonomous: true

must_haves:
  truths:
    - "Gateway deploy workflow installs dependencies with frozen lockfile without errors"
    - "Wrangler bundles cloud/gateway/gateway-entry.ts with all OSS imports resolved"
    - "Staging deploy targets staging-api.feelr.dev via wrangler.cloud.toml --env staging"
    - "Production deploy targets api.feelr.dev via wrangler.cloud.toml --env production"
  artifacts:
    - path: ".github/workflows/gateway.yml"
      provides: "Cloud gateway deploy workflow (staging + production)"
      contains: "wrangler.cloud.toml"
    - path: "cloud/gateway/package.json"
      provides: "Cloud gateway package with pinned stripe dependency"
      contains: "stripe"
  key_links:
    - from: ".github/workflows/gateway.yml"
      to: "cloud/gateway/wrangler.cloud.toml"
      via: "wrangler-action command flag"
      pattern: "wrangler.cloud.toml"
    - from: "cloud/gateway/wrangler.cloud.toml"
      to: "cloud/gateway/gateway-entry.ts"
      via: "main entry point"
      pattern: 'main = "gateway-entry.ts"'
---

<objective>
Fix the cloud gateway deploy workflow so it builds and deploys to Cloudflare Workers successfully.

Purpose: The gateway is the core API service. Its deploy workflow must reliably install dependencies (using the root lockfile created in Phase 30-02), bundle the cloud entry point via wrangler, and deploy to both staging and production environments.

Output: A working gateway.yml workflow and a pinned cloud/gateway/package.json.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pipeline-foundations/30-02-SUMMARY.md
@.github/workflows/gateway.yml
@cloud/gateway/package.json
@cloud/gateway/wrangler.cloud.toml
@cloud/gateway/gateway-entry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin stripe dependency and verify gateway workflow configuration</name>
  <files>cloud/gateway/package.json, .github/workflows/gateway.yml</files>
  <action>
1. In `cloud/gateway/package.json`, pin the stripe dependency to a specific version instead of `"latest"`. Run `pnpm view stripe version` to get the current latest version, then replace `"latest"` with `"^{version}"` (e.g., `"^17.5.0"` or whatever the current version is). Using `"latest"` causes lockfile instability because pnpm resolves it differently each time.

2. In `.github/workflows/gateway.yml`, verify and fix the workflow:
   - The `pnpm install --frozen-lockfile` step should work now that root lockfile exists (Phase 30-02)
   - Ensure the wrangler-action steps use `packageManager: pnpm` (already present)
   - The `workingDirectory: cloud/gateway` is correct
   - The `command: deploy -c wrangler.cloud.toml --env staging/production` is correct

3. No turbo build step is needed -- wrangler bundles the TypeScript entry point directly via esbuild. The workflow correctly skips turbo and goes straight to wrangler deploy.

4. After modifying package.json, regenerate the lockfile: `pnpm install --no-frozen-lockfile` from the repo root, then verify the lockfile is valid.
  </action>
  <verify>
- `cat cloud/gateway/package.json` shows stripe with a pinned semver range (not "latest")
- `pnpm install --frozen-lockfile` succeeds from repo root
- `.github/workflows/gateway.yml` contains `packageManager: pnpm` in both wrangler-action steps
- `.github/workflows/gateway.yml` references `wrangler.cloud.toml` in both deploy commands
  </verify>
  <done>
cloud/gateway/package.json has a pinned stripe version. The lockfile is consistent. gateway.yml is correctly configured to install deps with frozen lockfile and deploy via wrangler-action to both staging and production.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate gateway build chain end-to-end locally</name>
  <files>cloud/gateway/package.json</files>
  <action>
1. From repo root, run `pnpm install --frozen-lockfile` to verify clean install.

2. Verify that wrangler can resolve the gateway-entry.ts imports by running a dry-run build. From the cloud/gateway directory, run:
   ```
   npx wrangler deploy -c wrangler.cloud.toml --env staging --dry-run --outdir dist
   ```
   This bundles the entry point without deploying. If it fails, the import paths in gateway-entry.ts need fixing.

   Note: This may fail if Cloudflare credentials aren't configured locally. If so, instead verify the TypeScript compilation:
   ```
   npx tsc --noEmit -p cloud/gateway/tsconfig.json
   ```
   This validates that all imports in gateway-entry.ts resolve correctly (the tsconfig includes `../../oss/apps/gateway/src/**/*.ts`).

3. If the dry-run or typecheck reveals issues (e.g., missing types, unresolvable imports), fix them in the relevant files.
  </action>
  <verify>
Either `wrangler deploy --dry-run` succeeds with bundled output in dist/, or `tsc --noEmit -p cloud/gateway/tsconfig.json` exits 0 with no type errors.
  </verify>
  <done>
The gateway build chain is validated: wrangler can bundle cloud/gateway/gateway-entry.ts with all its OSS imports, or TypeScript compilation confirms all imports resolve. The workflow is ready to deploy when pushed to the cloud repo.
  </done>
</task>

</tasks>

<verification>
1. `cloud/gateway/package.json` has stripe pinned to a semver range
2. `pnpm install --frozen-lockfile` succeeds from repo root
3. Gateway entry point compiles without errors (wrangler dry-run or tsc --noEmit)
4. `.github/workflows/gateway.yml` references correct secrets: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID
5. Both staging and production deploy jobs use `wrangler.cloud.toml` with correct --env flags
</verification>

<success_criteria>
The cloud gateway deploy workflow is correctly configured: dependencies install cleanly with frozen lockfile, wrangler can bundle the cloud entry point with all OSS imports resolved, and both staging/production deploy jobs target the correct environments via wrangler.cloud.toml.
</success_criteria>

<output>
After completion, create `.planning/phases/31-cloud-deploy-workflows/31-01-SUMMARY.md`
</output>
