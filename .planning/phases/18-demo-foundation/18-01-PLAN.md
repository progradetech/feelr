---
phase: 18-demo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/src/lib/demo-context.tsx
  - apps/dashboard/src/components/providers.tsx
  - apps/dashboard/src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Calling enterDemo() from any component sets isDemo to true and persists across page navigations"
    - "Calling exitDemo() clears demo mode; closing the tab clears sessionStorage automatically"
    - "useDemo() returns the correct isDemo value in any component wrapped by the root layout"
  artifacts:
    - path: "apps/dashboard/src/lib/demo-context.tsx"
      provides: "DemoContext, DemoProvider, useDemo hook"
      exports: ["DemoContext", "DemoProvider", "useDemo"]
    - path: "apps/dashboard/src/components/providers.tsx"
      provides: "Client component wrapper hosting DemoProvider for server component root layout"
      exports: ["Providers"]
    - path: "apps/dashboard/src/app/layout.tsx"
      provides: "Root layout with Providers wrapper around children"
      contains: "Providers"
  key_links:
    - from: "apps/dashboard/src/app/layout.tsx"
      to: "apps/dashboard/src/components/providers.tsx"
      via: "import and render Providers wrapping children"
      pattern: "<Providers>"
    - from: "apps/dashboard/src/components/providers.tsx"
      to: "apps/dashboard/src/lib/demo-context.tsx"
      via: "import and render DemoProvider"
      pattern: "<DemoProvider>"
---

<objective>
Create the DemoContext React provider with sessionStorage-backed `isDemo` flag, plus a Providers wrapper component so the server-component root layout can host client-side context providers.

Purpose: Phase 19 (dashboard demo mode) and Phase 21 (interactive demo) need a global `isDemo` flag to switch data sources. This plan builds the context infrastructure they consume.
Output: `demo-context.tsx` (context + provider + hook), `providers.tsx` (client wrapper), updated `layout.tsx` (wires Providers around children).
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-demo-foundation/18-RESEARCH.md
@apps/dashboard/src/app/layout.tsx
@apps/dashboard/src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DemoContext provider with sessionStorage persistence</name>
  <files>apps/dashboard/src/lib/demo-context.tsx</files>
  <action>
Create `apps/dashboard/src/lib/demo-context.tsx` with the following:

1. Add `'use client'` directive at the top (required for useState, useEffect, useCallback).

2. Import `createContext`, `use`, `useState`, `useCallback`, `useEffect` from React.

3. Define a `STORAGE_KEY` constant set to `'feelr_demo_mode'`.

4. Define `DemoContextValue` interface with:
   - `isDemo: boolean`
   - `enterDemo: () => void`
   - `exitDemo: () => void`

5. Export `DemoContext = createContext<DemoContextValue>()` with default value `{ isDemo: false, enterDemo: () => {}, exitDemo: () => {} }`.

6. Export `DemoProvider` component that:
   - Accepts `{ children: React.ReactNode }` props
   - Uses `useState` with lazy initializer: `typeof window === 'undefined' ? false : sessionStorage.getItem(STORAGE_KEY) === 'true'`
   - Uses `useEffect` to sync `isDemo` to sessionStorage: if `true`, `setItem(STORAGE_KEY, 'true')`; if `false`, `removeItem(STORAGE_KEY)`. Guard with `typeof window === 'undefined'` check.
   - Wraps `enterDemo` and `exitDemo` in `useCallback`
   - Returns `<DemoContext value={{ isDemo, enterDemo, exitDemo }}>{children}</DemoContext>` — use React 19's direct context rendering (NOT `.Provider`)

7. Export `useDemo()` convenience hook that returns `use(DemoContext)` (React 19 `use` API).

IMPORTANT: Do NOT use `<DemoContext.Provider>` — it is deprecated in React 19. Use `<DemoContext value={}>` directly. Follow the existing `auth.ts` SSR guard pattern (`typeof window === 'undefined'` check).
  </action>
  <verify>
Run `cd /home/eternaldays/claudeRepos/feelr && npx tsc --noEmit -p apps/dashboard/tsconfig.json` — file compiles without errors. Verify the file exports DemoContext, DemoProvider, and useDemo by grepping: `grep -n 'export' apps/dashboard/src/lib/demo-context.tsx`.
  </verify>
  <done>demo-context.tsx exists with DemoContext, DemoProvider, and useDemo exported. Uses sessionStorage with STORAGE_KEY 'feelr_demo_mode'. Uses React 19 direct context rendering. Has SSR guard for server-side rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Create Providers wrapper and wire into root layout</name>
  <files>apps/dashboard/src/components/providers.tsx, apps/dashboard/src/app/layout.tsx</files>
  <action>
**File 1: `apps/dashboard/src/components/providers.tsx`**

Create a new client component:

1. Add `'use client'` directive.
2. Import `DemoProvider` from `@/lib/demo-context`.
3. Export `Providers` component accepting `{ children: React.ReactNode }`.
4. Return `<DemoProvider>{children}</DemoProvider>`.

This wrapper exists so the root layout stays a server component (preserving the `metadata` export). Future providers (e.g., SWRConfig, ThemeProvider) can be added here.

**File 2: Update `apps/dashboard/src/app/layout.tsx`**

1. Add import: `import { Providers } from '@/components/providers';`
2. Wrap `{children}` with `<Providers>`: change `{children}` to `<Providers>{children}</Providers>`.
3. Keep the `<Toaster>` OUTSIDE `<Providers>` (Toaster is independent, does not need demo context).
4. Do NOT add `'use client'` — the layout MUST remain a server component for the `metadata` export.

The resulting JSX in `layout.tsx` body should be:
```
<body className="bg-zinc-950 text-zinc-50 antialiased">
  <Providers>{children}</Providers>
  <Toaster theme="dark" />
</body>
```
  </action>
  <verify>
Run `cd /home/eternaldays/claudeRepos/feelr && npx tsc --noEmit -p apps/dashboard/tsconfig.json` — both files compile without errors. Verify layout.tsx still exports metadata: `grep 'export const metadata' apps/dashboard/src/app/layout.tsx`. Verify Providers wraps children: `grep 'Providers' apps/dashboard/src/app/layout.tsx`.
  </verify>
  <done>providers.tsx exists as a 'use client' component wrapping DemoProvider. layout.tsx imports and renders Providers around children while remaining a server component with metadata export intact. Toaster stays outside Providers.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/dashboard/tsconfig.json` passes with zero errors
2. `apps/dashboard/src/lib/demo-context.tsx` exports `DemoContext`, `DemoProvider`, `useDemo`
3. `apps/dashboard/src/components/providers.tsx` exports `Providers`
4. `apps/dashboard/src/app/layout.tsx` imports `Providers` and wraps `{children}` with it
5. `apps/dashboard/src/app/layout.tsx` still exports `metadata` (server component)
6. No `'use client'` directive in `layout.tsx`
7. `demo-context.tsx` uses `sessionStorage` with key `'feelr_demo_mode'`
8. `demo-context.tsx` uses `<DemoContext value={}>` not `<DemoContext.Provider>`
</verification>

<success_criteria>
- DemoContext provider is available app-wide via root layout -> Providers -> DemoProvider
- useDemo() hook returns { isDemo, enterDemo, exitDemo } from any component
- sessionStorage persists isDemo flag with key 'feelr_demo_mode'
- Root layout remains a server component with working metadata export
- TypeScript compilation passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-demo-foundation/18-01-SUMMARY.md`
</output>
