---
phase: 18-demo-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/src/lib/types.ts
  - apps/dashboard/src/lib/demo-data/keys.ts
  - apps/dashboard/src/lib/demo-data/connectors.ts
  - apps/dashboard/src/lib/demo-data/usage.ts
  - apps/dashboard/src/lib/demo-data/overview.ts
  - apps/dashboard/src/lib/demo-data/chains.ts
  - apps/dashboard/src/lib/demo-data/index.ts
autonomous: true

must_haves:
  truths:
    - "Mock data fixtures exist for all 5 data domains and conform to existing TypeScript types"
    - "Each fixture can be imported independently or via the barrel export"
    - "Fixture data is internally consistent (overview counts match keys/connectors fixture lengths)"
  artifacts:
    - path: "apps/dashboard/src/lib/types.ts"
      provides: "ChainHistoryEntry interface added alongside existing types"
      contains: "ChainHistoryEntry"
    - path: "apps/dashboard/src/lib/demo-data/keys.ts"
      provides: "DEMO_KEYS typed as ApiKey[]"
      exports: ["DEMO_KEYS"]
    - path: "apps/dashboard/src/lib/demo-data/connectors.ts"
      provides: "DEMO_CONNECTORS typed as ConnectorStatus[]"
      exports: ["DEMO_CONNECTORS"]
    - path: "apps/dashboard/src/lib/demo-data/usage.ts"
      provides: "DEMO_USAGE typed as UsageResponse, DEMO_RATE_LIMITS typed as RateLimitInfo[]"
      exports: ["DEMO_USAGE", "DEMO_RATE_LIMITS"]
    - path: "apps/dashboard/src/lib/demo-data/overview.ts"
      provides: "DEMO_OVERVIEW typed as OverviewData"
      exports: ["DEMO_OVERVIEW"]
    - path: "apps/dashboard/src/lib/demo-data/chains.ts"
      provides: "DEMO_CHAINS typed as ChainHistoryEntry[]"
      exports: ["DEMO_CHAINS"]
    - path: "apps/dashboard/src/lib/demo-data/index.ts"
      provides: "Barrel re-export of all fixtures"
      exports: ["DEMO_KEYS", "DEMO_CONNECTORS", "DEMO_USAGE", "DEMO_RATE_LIMITS", "DEMO_OVERVIEW", "DEMO_CHAINS"]
  key_links:
    - from: "apps/dashboard/src/lib/demo-data/keys.ts"
      to: "apps/dashboard/src/lib/types.ts"
      via: "imports ApiKey type"
      pattern: "import.*ApiKey.*from.*types"
    - from: "apps/dashboard/src/lib/demo-data/usage.ts"
      to: "apps/dashboard/src/lib/hooks/use-usage.ts"
      via: "imports RateLimitInfo type"
      pattern: "import.*RateLimitInfo.*from.*use-usage"
    - from: "apps/dashboard/src/lib/demo-data/chains.ts"
      to: "apps/dashboard/src/lib/types.ts"
      via: "imports ChainHistoryEntry type"
      pattern: "import.*ChainHistoryEntry.*from.*types"
---

<objective>
Create typed mock data fixtures for all 5 dashboard data domains (API keys, connectors, usage stats, overview, chain history) that conform to existing TypeScript types and can serve as drop-in data sources when demo mode is active.

Purpose: Phase 19 will wire these fixtures into SWR hooks so dashboard pages render realistic data without a backend. The fixtures must match hook return types exactly to avoid TypeScript errors during Phase 19 integration.
Output: `ChainHistoryEntry` type in `types.ts`, 5 fixture files in `demo-data/` directory, barrel `index.ts` re-exporting all fixtures.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-demo-foundation/18-RESEARCH.md
@apps/dashboard/src/lib/types.ts
@apps/dashboard/src/lib/hooks/use-usage.ts
@apps/dashboard/src/lib/hooks/use-keys.ts
@apps/dashboard/src/lib/hooks/use-connectors.ts
@apps/dashboard/src/lib/hooks/use-overview.ts
@apps/gateway/src/lib/chain-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ChainHistoryEntry type and create API keys + connectors fixtures</name>
  <files>apps/dashboard/src/lib/types.ts, apps/dashboard/src/lib/demo-data/keys.ts, apps/dashboard/src/lib/demo-data/connectors.ts</files>
  <action>
**File 1: Update `apps/dashboard/src/lib/types.ts`**

Add `ChainHistoryEntry` interface at the end of the file, AFTER the existing `GatewayResponse` interface. Derive from the gateway's `ChainExecutionResult` and `ChainStepResult` types in `apps/gateway/src/lib/chain-types.ts`. Add `id`, `chain_name`, and `executed_at` fields that a history API would provide:

```typescript
// Chain history types (derived from gateway ChainExecutionResult)
export interface ChainHistoryEntry {
  id: string;
  chain_name: string;
  success: boolean;
  steps_executed: number;
  steps_total: number;
  total_duration_ms: number;
  executed_at: string;
  steps: Array<{
    step_id: string;
    skipped: boolean;
    error: string | null;
    duration_ms: number;
  }>;
}
```

**File 2: Create `apps/dashboard/src/lib/demo-data/keys.ts`**

Create the `demo-data/` directory if it doesn't exist.

Export `DEMO_KEYS` as `ApiKey[]` with 3 entries using `satisfies` assertion:
1. `short_token: 'flr_a1b2'`, `label: 'production-agent'`, `created_at: '2026-01-15T10:30:00Z'`, `last_used_at: '2026-02-10T08:45:00Z'`
2. `short_token: 'flr_c3d4'`, `label: 'staging-bot'`, `created_at: '2026-01-20T14:00:00Z'`, `last_used_at: '2026-02-09T22:15:00Z'`
3. `short_token: 'flr_e5f6'`, `label: null`, `created_at: '2026-02-01T09:00:00Z'`, `last_used_at: null`

Use `import type { ApiKey } from '@/lib/types'`. Use `satisfies ApiKey[]` (not `as ApiKey[]`) for compile-time validation.

**File 3: Create `apps/dashboard/src/lib/demo-data/connectors.ts`**

Export `DEMO_CONNECTORS` as `ConnectorStatus[]` with 4 entries (one per known connector):
1. `{ name: 'github', status: 'connected' }`
2. `{ name: 'slack', status: 'connected' }`
3. `{ name: 'stripe', status: 'needs_reauth' }`
4. `{ name: 'discord', status: 'not_connected' }`

This gives a realistic mix of all 3 status states. 2 connected matches DEMO_OVERVIEW's `connected_services: 2`.

Use `import type { ConnectorStatus } from '@/lib/types'`. Use `satisfies ConnectorStatus[]`.
  </action>
  <verify>
Run `cd /home/eternaldays/claudeRepos/feelr && npx tsc --noEmit -p apps/dashboard/tsconfig.json` — all files compile. Verify `satisfies` assertions catch type mismatches: `grep 'satisfies' apps/dashboard/src/lib/demo-data/keys.ts apps/dashboard/src/lib/demo-data/connectors.ts`. Verify ChainHistoryEntry exported: `grep 'ChainHistoryEntry' apps/dashboard/src/lib/types.ts`.
  </verify>
  <done>ChainHistoryEntry interface exists in types.ts. DEMO_KEYS (3 ApiKey entries) and DEMO_CONNECTORS (4 ConnectorStatus entries with mix of all 3 states) exist with satisfies type assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Create usage, overview, chains fixtures and barrel export</name>
  <files>apps/dashboard/src/lib/demo-data/usage.ts, apps/dashboard/src/lib/demo-data/overview.ts, apps/dashboard/src/lib/demo-data/chains.ts, apps/dashboard/src/lib/demo-data/index.ts</files>
  <action>
**File 1: Create `apps/dashboard/src/lib/demo-data/usage.ts`**

Export TWO fixtures:

`DEMO_USAGE` typed as `UsageResponse` with `satisfies`:
- `window: '24h'`
- `buckets`: Array of 24 `UsageBucket` entries, one per hour for a realistic 24h period. Use hardcoded ISO 8601 timestamps (NOT `new Date()`). Shape a realistic traffic curve: low overnight (0-6 UTC: 2-8 requests), ramping up morning (7-12: 15-45 requests), peak afternoon (13-17: 50-80 requests), tapering evening (18-23: 30-12 requests). Each bucket has `time_bucket` (ISO string), `total_requests`, `error_count` (1-3 for some buckets, 0 for most), `avg_duration_ms` (120-350 range).
- `filters: {}`

`DEMO_RATE_LIMITS` typed as `RateLimitInfo[]` with `satisfies`:
- Import `RateLimitInfo` from `@/lib/hooks/use-usage` (NOT from types.ts — it lives in the hook file).
- 3 entries matching the 3 demo API keys:
  1. `{ api_key_short: 'flr_a1b2', label: 'production-agent', tier: 'pro', limit: 10000, usage_1m: 4521, throttle_24h: 0 }`
  2. `{ api_key_short: 'flr_c3d4', label: 'staging-bot', tier: 'free', limit: 1000, usage_1m: 287, throttle_24h: 2 }`
  3. `{ api_key_short: 'flr_e5f6', label: null, tier: 'free', limit: 1000, usage_1m: 0, throttle_24h: 0 }`

Import `UsageResponse` and `UsageBucket` from `@/lib/types`.

**File 2: Create `apps/dashboard/src/lib/demo-data/overview.ts`**

Export `DEMO_OVERVIEW` typed as `OverviewData` with `satisfies`:
- `total_keys: 3` (matches DEMO_KEYS.length)
- `connected_services: 2` (matches number of 'connected' entries in DEMO_CONNECTORS — github + slack)
- `recent_usage.total_24h`: sum of the 24 hourly bucket totals from DEMO_USAGE (calculate the sum to be consistent, approximately 500-700 total)
- `recent_usage.hourly`: Array of 24 `{ hour: string, count: number }` entries matching the same curve shape as DEMO_USAGE buckets. Use the same hour labels (ISO strings matching the bucket time_buckets) and use `total_requests` values from DEMO_USAGE for consistency.

Import `OverviewData` from `@/lib/types`.

IMPORTANT: Make `total_24h` exactly equal the sum of all `hourly[].count` values, and make the hourly counts match the DEMO_USAGE bucket `total_requests` values. Internal consistency matters for a believable demo.

**File 3: Create `apps/dashboard/src/lib/demo-data/chains.ts`**

Export `DEMO_CHAINS` typed as `ChainHistoryEntry[]` with `satisfies`:
- 4 entries showing a mix of chain executions:
  1. Successful 3-step chain: `id: 'ch_001'`, `chain_name: 'github-to-slack'`, `success: true`, `steps_executed: 3`, `steps_total: 3`, `total_duration_ms: 1250`, `executed_at: '2026-02-10T14:30:00Z'`, steps array with 3 step entries (none skipped, no errors)
  2. Successful 2-step chain with 1 skipped: `id: 'ch_002'`, `chain_name: 'stripe-invoice-notify'`, `success: true`, `steps_executed: 1`, `steps_total: 2`, `total_duration_ms: 890`, `executed_at: '2026-02-10T12:15:00Z'`, steps array: first step executed, second skipped
  3. Failed 4-step chain: `id: 'ch_003'`, `chain_name: 'discord-github-sync'`, `success: false`, `steps_executed: 2`, `steps_total: 4`, `total_duration_ms: 2100`, `executed_at: '2026-02-09T18:45:00Z'`, steps: first 2 executed (second has error: 'Rate limit exceeded'), last 2 skipped
  4. Successful 2-step chain: `id: 'ch_004'`, `chain_name: 'issue-triage'`, `success: true`, `steps_executed: 2`, `steps_total: 2`, `total_duration_ms: 650`, `executed_at: '2026-02-09T09:00:00Z'`, steps: both executed, no errors

Import `ChainHistoryEntry` from `@/lib/types`.

**File 4: Create `apps/dashboard/src/lib/demo-data/index.ts`**

Barrel file re-exporting all fixtures:
```typescript
export { DEMO_KEYS } from './keys';
export { DEMO_CONNECTORS } from './connectors';
export { DEMO_USAGE, DEMO_RATE_LIMITS } from './usage';
export { DEMO_OVERVIEW } from './overview';
export { DEMO_CHAINS } from './chains';
```
  </action>
  <verify>
Run `cd /home/eternaldays/claudeRepos/feelr && npx tsc --noEmit -p apps/dashboard/tsconfig.json` — all 7 files compile without errors. Verify barrel exports: `grep 'export' apps/dashboard/src/lib/demo-data/index.ts` shows all 6 fixtures. Verify internal consistency: the overview `total_keys` equals 3, `connected_services` equals 2.
  </verify>
  <done>DEMO_USAGE (24-hour UsageResponse with realistic traffic curve), DEMO_RATE_LIMITS (3 RateLimitInfo entries matching demo keys), DEMO_OVERVIEW (OverviewData consistent with keys/connectors counts), and DEMO_CHAINS (4 ChainHistoryEntry entries with success/failure/skip mix) all exist with satisfies type assertions. Barrel index.ts re-exports all 6 fixture constants.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/dashboard/tsconfig.json` passes with zero errors
2. `apps/dashboard/src/lib/types.ts` contains `ChainHistoryEntry` interface
3. `apps/dashboard/src/lib/demo-data/keys.ts` exports `DEMO_KEYS` as `ApiKey[]` with 3 entries
4. `apps/dashboard/src/lib/demo-data/connectors.ts` exports `DEMO_CONNECTORS` as `ConnectorStatus[]` with 4 entries
5. `apps/dashboard/src/lib/demo-data/usage.ts` exports `DEMO_USAGE` as `UsageResponse` and `DEMO_RATE_LIMITS` as `RateLimitInfo[]`
6. `apps/dashboard/src/lib/demo-data/overview.ts` exports `DEMO_OVERVIEW` as `OverviewData`
7. `apps/dashboard/src/lib/demo-data/chains.ts` exports `DEMO_CHAINS` as `ChainHistoryEntry[]` with 4 entries
8. `apps/dashboard/src/lib/demo-data/index.ts` re-exports all 6 fixture constants
9. All fixtures use `satisfies` (not `as`) for compile-time type validation
10. `DEMO_OVERVIEW.total_keys` equals `DEMO_KEYS.length` (3)
11. `DEMO_OVERVIEW.connected_services` equals count of 'connected' in DEMO_CONNECTORS (2)
12. `DEMO_RATE_LIMITS[].api_key_short` values match `DEMO_KEYS[].short_token` values
13. `RateLimitInfo` imported from `@/lib/hooks/use-usage` (not from types.ts)
</verification>

<success_criteria>
- All 5 data domains have typed fixture files with realistic demo data
- ChainHistoryEntry type exists in types.ts for future chain history dashboard page
- All fixtures compile against existing TypeScript types with zero errors
- Fixture data is internally consistent across domains (counts, key references)
- Barrel index.ts enables single-import access to all fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/18-demo-foundation/18-02-SUMMARY.md`
</output>
