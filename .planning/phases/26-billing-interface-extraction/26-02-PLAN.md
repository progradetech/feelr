---
phase: 26-billing-interface-extraction
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - apps/gateway/src/billing/stripe/index.ts
  - apps/gateway/src/billing/stripe/stripe-client.ts
  - apps/gateway/src/billing/stripe/meter.ts
  - apps/gateway/src/billing/middleware.ts
  - apps/gateway/src/lib/types.ts
  - apps/gateway/src/runtime/factory.ts
  - apps/gateway/src/index.ts
  - apps/gateway/package.json
autonomous: true

must_haves:
  truths:
    - "StripeBillingProvider is isolated in billing/stripe/ with zero imports from gateway core outside billing/types.ts"
    - "Gateway typechecks and all tests pass without the stripe npm package"
    - "billing/stripe/ can be deleted without breaking the gateway build"
    - "STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET are removed from AppEnv.Bindings"
    - "BILLING_PROVIDER is an optional binding on AppEnv"
    - "factory.ts does NOT import from billing/stripe/"
    - "plan-enforcer.ts is deleted (replaced by middleware.ts from Plan 01)"
  artifacts:
    - path: "apps/gateway/src/billing/stripe/index.ts"
      provides: "StripeBillingProvider class"
      exports: ["StripeBillingProvider"]
    - path: "apps/gateway/src/billing/stripe/stripe-client.ts"
      provides: "Stripe client factory (moved from billing/stripe-client.ts)"
      exports: ["createStripeClient", "webCrypto"]
    - path: "apps/gateway/src/billing/stripe/meter.ts"
      provides: "Stripe meter event recording (moved from billing/meter.ts)"
      exports: ["recordMeterEvent"]
    - path: "apps/gateway/src/lib/types.ts"
      provides: "AppEnv with BILLING_PROVIDER optional binding, no STRIPE_* bindings"
      contains: "BILLING_PROVIDER"
  key_links:
    - from: "apps/gateway/src/billing/stripe/index.ts"
      to: "apps/gateway/src/billing/provider.ts"
      via: "implements BillingProvider interface"
      pattern: "implements BillingProvider"
    - from: "apps/gateway/src/billing/stripe/index.ts"
      to: "apps/gateway/src/billing/types.ts"
      via: "imports billing types (pure types only)"
      pattern: "from '../types'"
    - from: "apps/gateway/src/billing/middleware.ts"
      to: "apps/gateway/src/lib/types.ts"
      via: "reads BILLING_PROVIDER from AppEnv"
      pattern: "BILLING_PROVIDER"
---

<objective>
Extract all Stripe-specific billing code into `billing/stripe/` subdirectory, implement StripeBillingProvider, remove `stripe` from gateway package.json, and clean up AppEnv bindings.

Purpose: After this plan, the gateway has zero Stripe dependencies in its core. StripeBillingProvider exists as an isolated module that can be deleted without breaking the build. The cloud entry point (index.ts) does NOT register StripeBillingProvider yet -- that happens in Phase 27 when the cloud repo is created.

Output: `billing/stripe/` directory with StripeBillingProvider, cleaned AppEnv, removed stripe dependency
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-billing-interface-extraction/26-RESEARCH.md
@.planning/phases/26-billing-interface-extraction/26-01-SUMMARY.md
@apps/gateway/src/billing/plan-enforcer.ts
@apps/gateway/src/billing/meter.ts
@apps/gateway/src/billing/stripe-client.ts
@apps/gateway/src/billing/types.ts
@apps/gateway/src/billing/provider.ts
@apps/gateway/src/billing/middleware.ts
@apps/gateway/src/lib/types.ts
@apps/gateway/src/runtime/factory.ts
@apps/gateway/src/index.ts
@apps/gateway/src/self-hosted-entry.ts
@apps/gateway/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Stripe files into billing/stripe/ and create StripeBillingProvider</name>
  <files>
    apps/gateway/src/billing/stripe/index.ts,
    apps/gateway/src/billing/stripe/stripe-client.ts,
    apps/gateway/src/billing/stripe/meter.ts,
    apps/gateway/src/billing/plan-enforcer.ts,
    apps/gateway/src/billing/meter.ts,
    apps/gateway/src/billing/stripe-client.ts,
    apps/gateway/src/billing/middleware.ts
  </files>
  <action>
**Step 1: Create `billing/stripe/` directory and move files**

Create `apps/gateway/src/billing/stripe/stripe-client.ts`:
- Copy the content of `billing/stripe-client.ts` exactly as-is (createStripeClient, webCrypto exports)
- No changes needed -- this file already only imports from 'stripe'

Create `apps/gateway/src/billing/stripe/meter.ts`:
- Copy the content of `billing/meter.ts` but update the imports:
  - Change `import { createStripeClient } from './stripe-client'` to `import { createStripeClient } from './stripe-client'` (stays the same since relative path within stripe/)
  - REMOVE the `import type { AppEnv } from '../lib/types'` line -- meter.ts should NOT import from gateway core
  - REMOVE the `createStripeClientFromEnv()` function entirely -- it referenced AppEnv which is gateway core. The StripeBillingProvider will handle env-to-client creation.
  - Keep ONLY the `recordMeterEvent()` function and its `import Stripe from 'stripe'` import

**Step 2: Create `apps/gateway/src/billing/stripe/index.ts` -- StripeBillingProvider**

```typescript
import Stripe from 'stripe'
import type { BillingProvider, QuotaResult } from '../provider'
import type { CustomerBilling } from '../types'
import { PLAN_LIMITS } from '../types'
import { createStripeClient } from './stripe-client'
import { recordMeterEvent } from './meter'
```

Implement `StripeBillingProvider` class implementing BillingProvider:
- Constructor takes `secretKey: string`, creates `this.stripe = createStripeClient(secretKey)`
- `enforceQuota()`: reads billing from kvGet(`billing:${apiKeyShort}`), defaults to hatchling plan, checks currentMonthUsage against PLAN_LIMITS, returns `{ allowed: false, plan, limit }` if over quota, `{ allowed: true }` otherwise. On KV read error, return `{ allowed: true }` (best-effort).
- `recordUsage()`: if customerId exists, call `recordMeterEvent(this.stripe, customerId)`. Then if billing exists, increment currentMonthUsage and call kvPut. Wrap kvPut in `.catch(() => {})` for best-effort.

This mirrors the exact logic currently in `plan-enforcer.ts` but encapsulated in the provider class. The enforceQuota and recordUsage logic is extracted from the current plan-enforcer middleware.

**Step 3: Update `billing/middleware.ts`**

The middleware from Plan 01 currently uses `(c.env as any).BILLING_PROVIDER`. Update it to:
- Import `BillingProvider` type from `./provider`
- Read provider as: `const provider: BillingProvider = c.env.BILLING_PROVIDER ?? new NoopBillingProvider()`
- This will work after AppEnv is updated in Task 2.

Also, the middleware should no longer need to do its own KV billing lookup + usage increment -- that logic is now in the provider. Simplify the middleware's post-next() section:
- After `await next()`, read billing from KV, get customerId
- Call `provider.recordUsage(apiKeyShort, billing, customerId, kvPut)` wrapped in waitUntil + catch

**Step 4: Delete old files**

Delete these files (their logic has been moved):
- `apps/gateway/src/billing/plan-enforcer.ts`
- `apps/gateway/src/billing/meter.ts` (moved to stripe/)
- `apps/gateway/src/billing/stripe-client.ts` (moved to stripe/)
  </action>
  <verify>
Run `pnpm typecheck --filter @feelr/gateway` -- will likely fail because stripe is still in package.json and AppEnv still has STRIPE_* bindings. That is expected -- Task 2 handles the cleanup. But verify: no circular imports, all files in billing/stripe/ compile.
  </verify>
  <done>StripeBillingProvider exists in billing/stripe/index.ts. Old plan-enforcer.ts, meter.ts, and stripe-client.ts are deleted. New files exist in billing/stripe/. Middleware delegates quota + usage to provider.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up AppEnv, factory, and remove stripe dependency</name>
  <files>
    apps/gateway/src/lib/types.ts,
    apps/gateway/src/runtime/factory.ts,
    apps/gateway/src/index.ts,
    apps/gateway/package.json
  </files>
  <action>
**Step 1: Update `apps/gateway/src/lib/types.ts`**

- Add import: `import type { BillingProvider } from '../billing/provider'`
- REMOVE these two bindings from AppEnv.Bindings:
  ```
  STRIPE_SECRET_KEY?: string
  STRIPE_WEBHOOK_SECRET?: string
  ```
- ADD this binding to AppEnv.Bindings:
  ```
  /** Pluggable billing provider (undefined = NoopBillingProvider) */
  BILLING_PROVIDER?: BillingProvider
  ```
- Keep all other bindings exactly as they are

**Step 2: Update `apps/gateway/src/runtime/factory.ts`**

In `createCloudBindings()`, the function does NOT currently pass STRIPE_SECRET_KEY or STRIPE_WEBHOOK_SECRET through to the adapted bindings (it is not in the return object). Verify this is the case.

Since `RawCloudflareEnv` does not include STRIPE_* either, no changes should be needed to factory.ts. But verify by reading it carefully. If STRIPE_SECRET_KEY appeared in RawCloudflareEnv or createCloudBindings, remove it.

NOTE: Do NOT add StripeBillingProvider to factory.ts. The factory creates bindings for the public/OSS gateway. The cloud overlay in Phase 27 will register StripeBillingProvider in its own entry point. For now, BILLING_PROVIDER is simply absent (= undefined = NoopBillingProvider via middleware fallback).

**Step 3: Update `apps/gateway/src/index.ts` (cloud entry point)**

The current index.ts does not reference Stripe at all -- it just calls `createCloudBindings(rawEnv)`. Verify no changes needed.

Do NOT add StripeBillingProvider registration here. Per research Pitfall 4: the cloud entry point in Phase 27 handles that. The current index.ts stays as the public/OSS entry point.

**Step 4: Remove stripe from package.json**

Run: `pnpm remove stripe --filter @feelr/gateway`

This removes `"stripe": "^20.3.1"` from apps/gateway/package.json dependencies.

**Step 5: Verify everything compiles and tests pass**

Run in sequence:
1. `pnpm typecheck --filter @feelr/gateway` -- MUST pass. If it fails, there is a leaked Stripe type import somewhere outside billing/stripe/. Fix any issues.
2. `pnpm test --filter @feelr/gateway` -- ALL tests must pass.

If typecheck fails due to `billing/stripe/` files importing from 'stripe' and the package is removed: this is expected because the stripe/ files reference the now-removed package. This means the stripe/ directory's TypeScript won't compile without the package. This is acceptable -- the billing/stripe/ directory is cloud-only code that will have the stripe package available in the cloud workspace. For the public repo typecheck, you need to exclude billing/stripe/ from compilation.

To handle this: add `"exclude": ["src/billing/stripe/**"]` to `apps/gateway/tsconfig.json` so the public repo typecheck skips the cloud-only Stripe module. Read tsconfig.json first to understand the existing structure before modifying. This ensures `pnpm typecheck` passes without the stripe package.

Alternatively, if the gateway tsconfig already uses a file inclusion pattern that doesn't auto-include billing/stripe/, the exclude may not be needed. Check and handle accordingly.
  </action>
  <verify>
Run these commands in sequence:
1. `pnpm typecheck --filter @feelr/gateway` -- MUST pass with zero errors
2. `pnpm test --filter @feelr/gateway` -- ALL existing tests must pass
3. Verify stripe is NOT in package.json: `grep '"stripe"' apps/gateway/package.json` should return nothing
4. Verify STRIPE_SECRET_KEY is NOT in lib/types.ts: `grep 'STRIPE_SECRET_KEY' apps/gateway/src/lib/types.ts` should return nothing
5. Verify BILLING_PROVIDER IS in lib/types.ts: `grep 'BILLING_PROVIDER' apps/gateway/src/lib/types.ts` should match
6. Verify StripeBillingProvider isolation: temporarily rename billing/stripe/ to billing/stripe.bak, run `pnpm typecheck --filter @feelr/gateway` (must pass), then rename back
  </verify>
  <done>
- stripe removed from apps/gateway/package.json
- STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET removed from AppEnv.Bindings
- BILLING_PROVIDER added as optional binding on AppEnv
- factory.ts does NOT import from billing/stripe/
- Gateway typechecks and all tests pass without the stripe package
- billing/stripe/ can be deleted without breaking the build
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck --filter @feelr/gateway` passes
- `pnpm test --filter @feelr/gateway` passes with zero regressions
- `grep '"stripe"' apps/gateway/package.json` returns empty (no stripe dependency)
- `grep 'STRIPE_SECRET_KEY' apps/gateway/src/lib/types.ts` returns empty
- `grep 'STRIPE_WEBHOOK_SECRET' apps/gateway/src/lib/types.ts` returns empty
- `grep 'BILLING_PROVIDER' apps/gateway/src/lib/types.ts` matches
- `apps/gateway/src/billing/stripe/index.ts` exports StripeBillingProvider
- `apps/gateway/src/billing/plan-enforcer.ts` does NOT exist (deleted)
- `apps/gateway/src/billing/meter.ts` does NOT exist (moved to stripe/)
- `apps/gateway/src/billing/stripe-client.ts` does NOT exist (moved to stripe/)
- Renaming billing/stripe/ to billing/stripe.bak does not break typecheck
</verification>

<success_criteria>
All Stripe-specific code is isolated in billing/stripe/. The stripe npm package is removed from gateway dependencies. AppEnv has BILLING_PROVIDER instead of STRIPE_* bindings. The gateway builds, typechecks, and passes all tests without Stripe.
</success_criteria>

<output>
After completion, create `.planning/phases/26-billing-interface-extraction/26-02-SUMMARY.md`
</output>
