---
phase: 25-pre-split-audit-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - docs/deployment/SECRETS-INVENTORY.md
autonomous: true

must_haves:
  truths:
    - ".gitignore contains **/.env pattern that matches env files at any depth"
    - ".gitignore contains **/.env.* pattern that matches env variant files at any depth"
    - "git ls-files | grep '\\.env' returns zero results (no env files tracked)"
    - "A secrets inventory exists documenting every credential with a disposition decision"
    - "Every file/directory is classified as PUBLIC, EXCLUDE, or PARTIAL for the repo split"
  artifacts:
    - path: ".gitignore"
      provides: "Hardened gitignore with explicit deep-match env patterns"
      contains: "**/.env"
    - path: "docs/deployment/SECRETS-INVENTORY.md"
      provides: "Complete secrets inventory with public release audit decisions"
      contains: "Public Release Audit"
  key_links:
    - from: ".gitignore"
      to: "git check-ignore"
      via: "pattern matching"
      pattern: "\\*\\*/.env"
    - from: "docs/deployment/SECRETS-INVENTORY.md"
      to: "Phase 27 repo split"
      via: "file classification decisions"
      pattern: "EXCLUDE|PUBLIC|ROTATE"
---

<objective>
Harden `.gitignore` with explicit `**/.env` deep-match patterns and update the secrets inventory with public release audit decisions for every credential and sensitive file.

Purpose: AUDIT-02 requires `**/.env` in `.gitignore` and zero tracked env files. AUDIT-03 requires a documented secrets inventory identifying every credential, token, and sensitive file with a decision (rotate, exclude, or safe-to-publish). This plan satisfies both requirements and produces the file classification matrix needed for Phase 27's repo split.

Output: Updated `.gitignore` with `**/.env` patterns, updated `docs/deployment/SECRETS-INVENTORY.md` with public release audit section.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-pre-split-audit-cleanup/25-RESEARCH.md
@.gitignore
@docs/deployment/SECRETS-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden .gitignore with explicit deep-match env patterns</name>
  <files>.gitignore</files>
  <action>
1. Read the current `.gitignore` (it has `.env`, `.env.*`, `!.env.example` patterns).

2. Update the "Secrets / local dev" section to add explicit `**/.env` and `**/.env.*` deep-match patterns. Keep the existing `.env` and `.env.*` patterns for backward compatibility. The updated section should be:

```
# Secrets / local dev
.dev.vars
.dev.vars.*
.env
.env.*
**/.env
**/.env.*
!.env.example
```

The `**/.env` pattern is required by the Phase 25 success criteria ("`.gitignore` contains `**/.env` pattern"). While the bare `.env` pattern already matches at any depth in git, the explicit `**/.env` is required for clarity and to satisfy the auditable requirement.

3. Verify the patterns work:
   ```bash
   git check-ignore -v self-host/.env
   # Must show a match

   # Test deep nesting
   mkdir -p /tmp/feelr-test/deep/path
   touch /tmp/feelr-test/deep/path/.env
   cd /repo && git check-ignore -v /tmp/feelr-test/deep/path/.env  # This won't work outside repo
   # Instead, test in-repo:
   mkdir -p test-ignore-depth/deep
   touch test-ignore-depth/deep/.env
   git check-ignore -v test-ignore-depth/deep/.env
   # Must show match against **/.env pattern
   rm -rf test-ignore-depth
   ```

4. Verify no env files are tracked:
   ```bash
   git ls-files | grep '\.env'
   ```
   Must return empty (zero results). Research confirmed `self-host/.env` was never tracked.

5. Verify the `.env.example` exception still works:
   ```bash
   git check-ignore -v .env.example
   ```
   Should NOT be ignored (the `!.env.example` negation pattern must still work).
  </action>
  <verify>
- `grep '\\*\\*/.env' .gitignore` returns the `**/.env` line
- `git check-ignore -v self-host/.env` returns a match
- `git ls-files | grep '\.env'` returns empty
- `git check-ignore .env.example` returns empty (not ignored)
  </verify>
  <done>
`.gitignore` contains `**/.env` and `**/.env.*` patterns. No `.env` files are tracked in git. The `.env.example` negation still works.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create public release secrets inventory with file classifications</name>
  <files>docs/deployment/SECRETS-INVENTORY.md</files>
  <action>
1. Read the existing `docs/deployment/SECRETS-INVENTORY.md` which already documents every secret with rotation procedures.

2. Add a new "Section 5: Public Release Audit" to the END of the existing file (preserve all existing content). This section satisfies AUDIT-03's requirement for a documented secrets inventory with disposition decisions.

The new section must contain:

**A. Credential Disposition Table** — For every secret in the existing inventory, record:
| Secret | Decision | Reason | Action Timing |
|--------|----------|--------|---------------|
| CLOUDFLARE_API_TOKEN | ROTATE | Was in private repo accessible to collaborators | After repo split (Phase 27) |
| CLOUDFLARE_ACCOUNT_ID | SAFE | Not a secret; identifies account but grants no access alone | None |
| SWA_DASHBOARD_DEPLOYMENT_TOKEN | ROTATE | Was in private repo accessible to collaborators | After repo split (Phase 27) |
| SWA_DOCS_DEPLOYMENT_TOKEN | ROTATE | Was in private repo accessible to collaborators | After repo split (Phase 27) |
| HOMEBREW_TAP_GITHUB_TOKEN | ROTATE | PAT with repo scope; was in private repo | After repo split (Phase 27) |
| GITHUB_TOKEN | SAFE | Auto-generated per workflow run; expires after run | None |
| ENCRYPTION_KEY | ROTATE | Cloudflare Worker secret; rotate as precaution | After repo split (Phase 27) |
| ADMIN_TOKEN | ROTATE | Cloudflare Worker secret; rotate as precaution | After repo split (Phase 27) |
| SLACK_CLIENT_ID | ROTATE | Cloudflare Worker secret; rotate as precaution | After repo split (Phase 27) |
| SLACK_CLIENT_SECRET | ROTATE | Cloudflare Worker secret; rotate as precaution | After repo split (Phase 27) |
| STRIPE_SECRET_KEY | ROTATE | Cloudflare Worker secret; most sensitive | After repo split (Phase 27) |
| STRIPE_WEBHOOK_SECRET | ROTATE | Cloudflare Worker secret; rotate as precaution | After repo split (Phase 27) |

**B. File/Directory Classification Matrix** — For every top-level path, record:
| Path | Decision | Reason |
|------|----------|--------|
| `apps/` | PUBLIC | Application source code (gateway, dashboard, docs) |
| `cli/` | PUBLIC | Go CLI source code |
| `connectors/` | PUBLIC | Connector source code + template |
| `packages/` | PUBLIC | Shared packages (tsconfig, etc.) |
| `chains/` | PUBLIC | Example composable action chains |
| `self-host/` | PUBLIC | Self-hosting config + Docker (minus .env which is untracked) |
| `scripts/` | PUBLIC | Build/deploy scripts |
| `.github/workflows/` | PARTIAL | CI workflows public; deploy workflows move to cloud repo |
| `.goreleaser.yaml` | PUBLIC | CLI build configuration |
| `README.md` | PUBLIC | Project readme |
| `CONTRIBUTING.md` | PUBLIC | Contribution guide |
| `LICENSE` | PUBLIC | MIT license |
| `.gitignore` | PUBLIC | Git ignore rules |
| `.gitleaks.toml` | PUBLIC | Secret scanning config |
| `turbo.json` | PUBLIC | Turborepo config |
| `pnpm-workspace.yaml` | PUBLIC | Workspace config |
| `package.json` | PUBLIC | Root package.json |
| `tsconfig.base.json` | PUBLIC | Shared TypeScript config |
| `apps/gateway/wrangler.toml` | PUBLIC | KV/D1 IDs are safe per Cloudflare docs; production env blocks to be stripped in Phase 27 |
| `.planning/` | EXCLUDE | 246 internal planning docs (phases, research, milestones) |
| `feelr-strategy.md` | EXCLUDE | Competitive strategy, pricing, go-to-market |
| `docs/deployment/SECRETS-INVENTORY.md` | EXCLUDE | Production credential metadata + rotation procedures |
| `docs/deployment/RUNBOOK.md` | EXCLUDE | Production deployment procedures with infrastructure details |
| `assets/` | PUBLIC | Logo SVGs (currently untracked; to be committed for public repo) |

**C. Notes for Phase 27:**
- All secrets marked ROTATE must be rotated AFTER the public repo is created
- `.planning/` exclusion is handled by the fresh snapshot approach (not copying .planning/ to public repo)
- `wrangler.toml` production/staging environment blocks should be stripped in the public repo snapshot
- Deploy workflows in `.github/workflows/` that reference cloud-specific secrets move to `feelr-cloud` repo

3. Do NOT modify any existing sections (1-4). Only append Section 5.
  </action>
  <verify>
- `grep 'Public Release Audit' docs/deployment/SECRETS-INVENTORY.md` returns a match
- The file still contains all original sections (1-4)
- The Credential Disposition Table contains all 12 secrets from the existing inventory
- The File Classification Matrix covers all major paths from the research
- Every entry has a Decision (ROTATE, SAFE, EXCLUDE, PUBLIC, PARTIAL) and a Reason
  </verify>
  <done>
The secrets inventory has a "Public Release Audit" section with credential disposition decisions for all 12 secrets and file classification decisions for every directory/file in the repo. Every credential is marked ROTATE, SAFE, or EXCLUDE. Every file path is marked PUBLIC, EXCLUDE, or PARTIAL with documented reasons.
  </done>
</task>

</tasks>

<verification>
- `.gitignore` contains `**/.env` pattern (success criteria #3)
- `git ls-files | grep '\.env'` returns zero results (success criteria #2)
- `git check-ignore -v self-host/.env` shows match
- Secrets inventory has disposition decision for every credential (success criteria #4)
- File classification matrix covers all repo paths
</verification>

<success_criteria>
1. `.gitignore` has `**/.env` and `**/.env.*` deep-match patterns
2. No `.env` files are tracked in git
3. Secrets inventory documents every credential with ROTATE/SAFE/EXCLUDE decision
4. File classification matrix covers all directories with PUBLIC/EXCLUDE/PARTIAL decision
5. Existing SECRETS-INVENTORY.md sections 1-4 are preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/25-pre-split-audit-cleanup/25-02-SUMMARY.md`
</output>
