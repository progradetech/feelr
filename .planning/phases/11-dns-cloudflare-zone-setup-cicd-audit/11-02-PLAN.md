---
phase: 11-dns-cloudflare-zone-setup-cicd-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yaml
  - .github/workflows/release.yml
autonomous: false
user_setup:
  - service: github
    why: "Homebrew tap repository creation and PAT for cross-repo push"
    env_vars:
      - name: HOMEBREW_TAP_GITHUB_TOKEN
        source: "GitHub > Settings > Developer settings > Personal Access Tokens > Fine-grained tokens > Create token scoped to andrewprograde/homebrew-feelr with contents:write permission. Then add as repo secret: progradetech/feelr > Settings > Secrets and variables > Actions > New repository secret"
    dashboard_config:
      - task: "Create andrewprograde/homebrew-feelr repository"
        location: "GitHub > New Repository > Owner: andrewprograde, Name: homebrew-feelr, Public, Initialize with README"

must_haves:
  truths:
    - "goreleaser check passes with no errors on .goreleaser.yaml"
    - "release.yml workflow passes HOMEBREW_TAP_GITHUB_TOKEN to GoReleaser"
    - ".goreleaser.yaml brews section references explicit token for tap push"
    - "andrewprograde/homebrew-feelr repository exists on GitHub"
    - "HOMEBREW_TAP_GITHUB_TOKEN secret exists in progradetech/feelr repo settings"
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "Fixed GoReleaser config with explicit tap token"
      contains: "HOMEBREW_TAP_GITHUB_TOKEN"
    - path: ".github/workflows/release.yml"
      provides: "Release workflow with correct env vars and working directory"
      contains: "HOMEBREW_TAP_GITHUB_TOKEN"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".goreleaser.yaml"
      via: "goreleaser-action invocation"
      pattern: "goreleaser/goreleaser-action"
    - from: ".goreleaser.yaml brews.repository.token"
      to: "HOMEBREW_TAP_GITHUB_TOKEN env var"
      via: "GoReleaser template expansion"
      pattern: "\\.Env\\.HOMEBREW_TAP_GITHUB_TOKEN"
---

<objective>
Audit and fix the existing GoReleaser-based release pipeline (release.yml + .goreleaser.yaml) so that pushing a version tag triggers a successful release with cross-compiled binaries and a Homebrew formula published to the tap repository.

Purpose: The release pipeline was created during v1.0 but has never been validated. Research identified three critical issues that would cause the Homebrew tap push to fail. Fixing these now ensures the CLI distribution pipeline works before v1.1 deployment phases add more CI/CD complexity.
Output: Working .goreleaser.yaml and release.yml with all identified issues resolved.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dns-cloudflare-zone-setup-cicd-audit/11-RESEARCH.md
@.goreleaser.yaml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .goreleaser.yaml and release.yml issues</name>
  <files>.goreleaser.yaml, .github/workflows/release.yml</files>
  <action>
    Fix all identified issues in the GoReleaser configuration:

    **Fix 1 (CRITICAL): Add explicit token to brews.repository**
    In `.goreleaser.yaml`, add the `token` field to `brews[0].repository`:
    ```yaml
    brews:
      - name: feelr
        repository:
          owner: andrewprograde
          name: homebrew-feelr
          token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    ```
    This is critical because without it, GoReleaser falls back to GITHUB_TOKEN which cannot push to a different repository.

    **Fix 2 (MEDIUM): Confirm repository owner is andrewprograde**
    Per user instructions ("ALWAYS use andrewprograde for all projects"), the tap owner should remain `andrewprograde`. The .goreleaser.yaml already has this correct. No change needed here.

    **Fix 3 (LOW): Add deprecation comment for brews section**
    Add a YAML comment above the `brews` section noting it's deprecated since GoReleaser v2.10 but still functional:
    ```yaml
    # NOTE: `brews` is deprecated since GoReleaser v2.10 in favor of `homebrew_casks`.
    # Still functional; migrate when v3 deprecation is announced.
    brews:
    ```
    Do NOT migrate to homebrew_casks -- keep brews for now (working config, no risk).

    **Fix 4: Add workdir to goreleaser-action**
    The goreleaser-action should specify `workdir: .` explicitly since the .goreleaser.yaml is at the repo root but the Go module is in `cli/`. The current config handles this via `dir: cli` in the builds section, which is correct. Verify the existing config is coherent -- no change needed unless the `dir` field is missing.

    **Fix 5: Ensure release.yml passes HOMEBREW_TAP_GITHUB_TOKEN**
    Verify `.github/workflows/release.yml` already passes `HOMEBREW_TAP_GITHUB_TOKEN` as an env var to the GoReleaser step. The current file already has this on line 31. Confirm it's correct.

    After making changes, validate locally:
    - Read the final .goreleaser.yaml and confirm it's valid YAML
    - Verify the token template string is exactly `"{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"`
    - Verify brews.repository has owner, name, and token fields
  </action>
  <verify>
    Read the modified .goreleaser.yaml and confirm:
    1. `brews[0].repository.token` field exists with value `"{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"`
    2. `brews[0].repository.owner` is `andrewprograde`
    3. `brews[0].repository.name` is `homebrew-feelr`
    4. Deprecation comment is present above `brews` section
    5. release.yml still passes HOMEBREW_TAP_GITHUB_TOKEN env var
  </verify>
  <done>.goreleaser.yaml has explicit tap token reference, deprecation comment, and owner is andrewprograde. release.yml correctly passes the token as env var.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create Homebrew tap repo and configure PAT secret</name>
  <action>
    The developer must perform two GitHub actions that Claude cannot automate (no gh CLI available):

    **Step 1: Create the Homebrew tap repository**
    - Go to https://github.com/new
    - Owner: `andrewprograde`
    - Repository name: `homebrew-feelr`
    - Visibility: Public (Homebrew taps must be public for `brew install` to work)
    - Check "Add a README file"
    - Click "Create repository"
    - Verify: https://github.com/andrewprograde/homebrew-feelr should load

    **Step 2: Create a Fine-Grained Personal Access Token**
    - Go to https://github.com/settings/personal-access-tokens/new
    - Token name: `feelr-goreleaser-tap`
    - Expiration: 90 days (or custom -- you'll need to rotate it)
    - Resource owner: `andrewprograde`
    - Repository access: "Only select repositories" > select `andrewprograde/homebrew-feelr`
    - Permissions > Repository permissions > Contents: Read and write
    - Click "Generate token"
    - Copy the token value

    **Step 3: Add token as repository secret**
    - Go to https://github.com/progradetech/feelr/settings/secrets/actions
    - Click "New repository secret"
    - Name: `HOMEBREW_TAP_GITHUB_TOKEN`
    - Value: paste the token from Step 2
    - Click "Add secret"
  </action>
  <resume-signal>Type "secrets configured" once you've created the tap repo and added the HOMEBREW_TAP_GITHUB_TOKEN secret. If you hit any issues (wrong org permissions, can't access settings), describe the problem.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate release pipeline with snapshot build</name>
  <files></files>
  <action>
    After the developer confirms secrets are configured, validate the GoReleaser config locally:

    1. Install goreleaser if not available:
       `go install github.com/goreleaser/goreleaser/v2@latest`
       (Requires Go toolchain which is already installed for the CLI)

    2. Validate config syntax:
       `goreleaser check`
       Expected: "config is valid" (may show deprecation warning for `brews` which is expected and acceptable)

    3. Run snapshot build (builds binaries without publishing):
       `goreleaser release --snapshot --skip=publish --clean`
       This tests the full build pipeline including cross-compilation for all 6 platform combos (linux/darwin x amd64/arm64, windows x amd64/arm64).

    4. Verify build artifacts:
       `ls dist/`
       Expected: Archive files for each platform combo, checksums.txt, metadata.json

    5. If goreleaser check or snapshot build fails, diagnose and fix the issue in .goreleaser.yaml.

    Note: The snapshot build cannot test the Homebrew tap push (that requires a real release). The tap push will be validated when the first real tag is pushed. The snapshot validates that the build, archive, and config parsing are all correct.
  </action>
  <verify>
    `goreleaser check` exits 0 (config valid).
    `ls dist/` shows archive files for linux_amd64, linux_arm64, darwin_amd64, darwin_arm64, windows_amd64, windows_arm64.
    dist/checksums.txt exists.
  </verify>
  <done>GoReleaser config validates cleanly and snapshot build produces all 6 platform archives plus checksums. Pipeline is ready for a real release.</done>
</task>

</tasks>

<verification>
- `.goreleaser.yaml` has `brews[0].repository.token` referencing HOMEBREW_TAP_GITHUB_TOKEN
- `.goreleaser.yaml` has `brews[0].repository.owner` set to `andrewprograde`
- `andrewprograde/homebrew-feelr` repository exists on GitHub
- `HOMEBREW_TAP_GITHUB_TOKEN` secret exists in progradetech/feelr repo settings
- `goreleaser check` passes
- `goreleaser release --snapshot --skip=publish --clean` builds successfully
</verification>

<success_criteria>
1. All identified GoReleaser config issues are fixed (token reference, deprecation comment)
2. Homebrew tap repo exists at andrewprograde/homebrew-feelr
3. PAT secret configured in repo settings
4. Snapshot build succeeds locally, producing archives for all 6 platforms
5. Pipeline is ready for a real v1.0.1 tag push to validate end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/11-dns-cloudflare-zone-setup-cicd-audit/11-02-SUMMARY.md`
</output>
