---
phase: 10-launch-prep
plan: 04
type: execute
wave: 2
depends_on: ["10-02", "10-03"]
files_modified:
  - LICENSE
  - CONTRIBUTING.md
  - README.md
  - connectors/_template/src/index.ts
  - connectors/_template/src/actions/items.ts
  - connectors/_template/src/actions/item-get.ts
  - connectors/_template/src/actions/item-create.ts
  - connectors/_template/src/__tests__/actions.test.ts
  - connectors/_template/README.md
  - connectors/_template/package.json
autonomous: true

must_haves:
  truths:
    - "MIT LICENSE file exists at repo root"
    - "CONTRIBUTING.md explains how to create a new connector with step-by-step instructions"
    - "README has marketing-forward hero section, badges, feature highlights, and install instructions"
    - "Connector template is a full working example with 3-4 actions, tests, and README"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "CONTRIBUTING.md"
      provides: "Contributor guidelines with connector development walkthrough"
      contains: "connector"
    - path: "README.md"
      provides: "Marketing-forward project README"
      contains: "feelr"
    - path: "connectors/_template/src/actions/items.ts"
      provides: "Full working list action example"
      contains: "items.list"
    - path: "connectors/_template/src/__tests__/actions.test.ts"
      provides: "Test examples for connector template"
      contains: "describe"
  key_links:
    - from: "README.md"
      to: "apps/docs"
      via: "link to documentation site"
      pattern: "feelr.dev/docs"
    - from: "CONTRIBUTING.md"
      to: "connectors/_template"
      via: "reference to connector template"
      pattern: "_template"
---

<objective>
Create open-source packaging: MIT LICENSE, CONTRIBUTING.md with connector development guide, marketing-forward README, and enhanced connector template as a full working example.

Purpose: Make the repository ready for public release and community contributions with professional documentation and a copy-and-modify connector template.
Output: LICENSE, CONTRIBUTING.md, README.md, enhanced connector template with 3-4 actions, tests, and README.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@connectors/_template/src/index.ts
@connectors/_template/package.json
@.planning/phases/10-launch-prep/10-02-PLAN.md
@.planning/phases/10-launch-prep/10-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LICENSE, CONTRIBUTING.md, and README.md</name>
  <files>
    LICENSE
    CONTRIBUTING.md
    README.md
  </files>
  <action>
    1. Create `LICENSE` at repo root:
       - MIT License (per CONTEXT.md locked decision, NOT Apache 2.0 from research)
       - Copyright (c) 2026 Feelr Contributors
       - Use the exact standard MIT text from choosealicense.com/licenses/mit/

    2. Create `CONTRIBUTING.md`:
       - Title: "Contributing to Feelr"
       - Sections:
         - **Getting Started**: Fork, clone, pnpm install, pnpm turbo typecheck
         - **Project Structure**: Brief overview of monorepo layout (apps/, packages/, connectors/, cli/)
         - **Creating a New Connector** (main section):
           1. Copy `connectors/_template/` to `connectors/your-connector/`
           2. Update package.json name to `@feelr/connector-your-name`
           3. Implement actions (explain ActionDefinition interface)
           4. Add tests
           5. Register in gateway (v1.ts import + registerConnector)
           6. Run `pnpm install && pnpm turbo typecheck`
           - Reference the template README for detailed examples
         - **Connector Guidelines**: Web Standard APIs only, @feelr/connector-sdk only dependency, agent-optimized descriptions, flat response normalization
         - **CLI Development**: Go 1.25+, `cd cli && go build .`, `go test ./...`
         - **Commit Convention**: `feat(scope):`, `fix(scope):`, `docs(scope):`, `test(scope):`
         - **Pull Requests**: Describe what and why, include test output, keep PRs focused
         - **No CLA required** (per CONTEXT.md decision: MIT covers it, low barrier)
       - Keep it concise and actionable. No bureaucratic process.

    3. Create `README.md` at repo root:
       - Per CONTEXT.md: Marketing-forward, designed for GitHub trending visibility
       - **Hero section**: Large header "Feelr", tagline "One line. Any API. Zero context overhead."
       - **Badges row**: License (MIT), GoReleaser, GitHub Actions, Go Report Card
       - **What is Feelr**: 2-3 sentences explaining the value proposition for AI agents
       - **Feature highlights** (bulleted with brief descriptions):
         - 4 connectors (GitHub, Slack, Stripe, Discord)
         - Composable action chains
         - Progressive CLI discovery (`feelr tools`)
         - Web dashboard
         - Self-hostable (Docker Compose)
         - 3 billing tiers (cloud) / unlimited (self-hosted)
       - **Quick Start** section: Two paths side by side or sequential
         - API path: 3 curl commands (get key, store cred, run action)
         - CLI path: install, init, auth, run
       - **Install** section: Show all 4 methods equally per CONTEXT.md:
         - Homebrew: `brew install andrewprograde/feelr/feelr`
         - curl: `curl -sSL https://feelr.dev/install.sh | sh`
         - go install: `go install github.com/andrewprograde/feelr/cli@latest`
         - GitHub Releases: link to releases page
       - **Documentation**: Link to feelr.dev/docs (from Plan 02)
       - **Self-Hosting**: Brief mention + link to self-host/ directory README
       - **Contributing**: Brief mention + link to CONTRIBUTING.md
       - **License**: MIT
       - Note: GIF demo mentioned in CONTEXT.md as an idea but creating actual GIFs is out of scope for this plan. Use a placeholder comment `<!-- TODO: Add GIF demo -->` or a code block showing terminal output as a static alternative.
  </action>
  <verify>
    All 3 files exist at repo root. LICENSE contains "MIT License". README has badges, feature highlights, install instructions for all 4 methods, and links to docs and CONTRIBUTING. CONTRIBUTING references the connector template.
  </verify>
  <done>
    MIT LICENSE file at repo root. CONTRIBUTING.md with connector development walkthrough and no CLA requirement. README.md is marketing-forward with hero section, badges, feature highlights, multi-method install instructions, and links to docs site and self-hosting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced connector template with full working example</name>
  <files>
    connectors/_template/src/index.ts
    connectors/_template/src/actions/items.ts
    connectors/_template/src/actions/item-get.ts
    connectors/_template/src/actions/item-create.ts
    connectors/_template/src/__tests__/actions.test.ts
    connectors/_template/README.md
    connectors/_template/package.json
  </files>
  <action>
    Per CONTEXT.md: "Full working example -- complete mock connector with 3-4 actions, tests, README. Copy-and-modify pattern for contributors."

    1. Create `connectors/_template/src/actions/items.ts`:
       - Export `itemsList` ActionDefinition:
         - name: 'items.list'
         - description: agent-optimized (50-100 tokens)
         - params: page (number, optional, default 1), per_page (number, optional, default 20), status (string, optional, default 'active')
         - returns: 'list'
         - handler: mock implementation returning placeholder items array with pagination meta
         - Includes commented-out real API call pattern showing fetch + error handling + flatten

    2. Create `connectors/_template/src/actions/item-get.ts`:
       - Export `itemGet` ActionDefinition:
         - name: 'item.get'
         - params: id (string, required)
         - returns: 'single'
         - handler: mock returning a single item object
         - Shows single-resource pattern (vs list pattern)

    3. Create `connectors/_template/src/actions/item-create.ts`:
       - Export `itemCreate` ActionDefinition:
         - name: 'item.create'
         - params: name (string, required), description (string, optional), tags (string, optional)
         - returns: 'single'
         - handler: mock returning the created item
         - Shows write/mutation pattern (POST body construction)

    4. Update `connectors/_template/src/index.ts`:
       - Import all 3 actions from ./actions/
       - Register all 3 in the actions map: 'items.list', 'item.get', 'item.create'
       - Remove the inline resourceList action (replaced by split files)
       - Keep the comprehensive header comment explaining how to create a connector
       - Export name as 'template', auth_type as 'bearer_token'

    5. Create `connectors/_template/src/__tests__/actions.test.ts`:
       - Use vitest (import { describe, it, expect } from 'vitest')
       - Test items.list: verify returns data array, meta with has_more, raw
       - Test item.get: verify returns single object with expected fields
       - Test item.create: verify required params produce created item
       - Test error case: demonstrate how to test error handling
       - These serve as EXAMPLES for contributors to follow when writing their own connector tests

    6. Update `connectors/_template/package.json`:
       - Add vitest as devDependency
       - Add test script: "test": "vitest run"
       - Keep existing dependencies (@feelr/connector-sdk)

    7. Create `connectors/_template/README.md`:
       - Title: "Feelr Connector Template"
       - Quick overview of what this template provides
       - Step-by-step: Copy directory, rename, implement actions, register
       - File structure explanation (src/index.ts, src/actions/, src/__tests__/)
       - Action patterns: list (paginated), get (single), create (mutation)
       - Testing patterns: mock context, assertion examples
       - Registration: how to wire into the gateway
       - Link back to CONTRIBUTING.md for full guidelines
  </action>
  <verify>
    `ls connectors/_template/src/actions/` shows 3 action files (items.ts, item-get.ts, item-create.ts). `connectors/_template/src/__tests__/actions.test.ts` exists. `connectors/_template/README.md` exists. The template index.ts imports and registers all 3 actions.
  </verify>
  <done>
    Connector template is a full working example with 3 actions (list, get, create), tests showing assertion patterns, and a README with step-by-step instructions. Contributors can copy the entire directory and modify to create a new connector.
  </done>
</task>

</tasks>

<verification>
- `LICENSE` file contains MIT License text
- `CONTRIBUTING.md` references connector template and has no CLA requirement
- `README.md` has hero section, badges, 4 install methods, links to docs and self-hosting
- Connector template has 3 action files, tests, and README
- Template index.ts registers all 3 actions
- No deferred ideas implemented (none were deferred)
</verification>

<success_criteria>
Repository has MIT LICENSE, contributor-friendly CONTRIBUTING.md with connector development walkthrough, marketing-forward README optimized for GitHub trending, and a full working connector template with 3 actions, tests, and documentation that contributors can copy and modify.
</success_criteria>

<output>
After completion, create `.planning/phases/10-launch-prep/10-04-SUMMARY.md`
</output>
