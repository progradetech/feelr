---
phase: 10-launch-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/docs/package.json
  - apps/docs/next.config.mjs
  - apps/docs/tsconfig.json
  - apps/docs/app/layout.tsx
  - apps/docs/app/page.mdx
  - apps/docs/app/docs/getting-started-api/page.mdx
  - apps/docs/app/docs/getting-started-cli/page.mdx
  - apps/docs/app/docs/auth/setup/page.mdx
  - apps/docs/app/docs/connectors/github/page.mdx
  - apps/docs/app/docs/connectors/slack/page.mdx
  - apps/docs/app/docs/connectors/stripe/page.mdx
  - apps/docs/app/docs/connectors/discord/page.mdx
  - apps/docs/app/docs/cli-reference/page.mdx
  - apps/docs/app/docs/_meta.ts
  - apps/docs/mdx-components.tsx
autonomous: true

must_haves:
  truths:
    - "Documentation site builds with static export and produces HTML files"
    - "Two quick-start guides exist: API-first (curl) and CLI-first (install + run)"
    - "Auth setup page explains API key creation and credential storage"
    - "Each connector has a reference page with action tables and examples"
    - "Self-hosting docs link to the repo (not duplicated on docs site)"
  artifacts:
    - path: "apps/docs/package.json"
      provides: "Nextra v4 docs site workspace package"
      contains: "nextra"
    - path: "apps/docs/app/docs/getting-started-api/page.mdx"
      provides: "API-first quick-start (curl path)"
      contains: "curl"
    - path: "apps/docs/app/docs/getting-started-cli/page.mdx"
      provides: "CLI-first quick-start (install + run path)"
      contains: "feelr run"
    - path: "apps/docs/app/docs/connectors/github/page.mdx"
      provides: "GitHub connector action reference"
      contains: "issues.list"
  key_links:
    - from: "apps/docs/next.config.mjs"
      to: "nextra"
      via: "withNextra wrapper"
      pattern: "nextra"
    - from: "apps/docs/app/layout.tsx"
      to: "nextra-theme-docs"
      via: "theme import"
      pattern: "nextra-theme-docs"
---

<objective>
Create the documentation site at apps/docs using Nextra v4 with quick-start guides, auth setup, and connector references.

Purpose: Provide comprehensive, developer-friendly documentation for Feelr at feelr.dev/docs covering both API and CLI paths to first working request.
Output: Static-exportable Nextra v4 docs site with all content pages.
</objective>

<execution_context>
@/home/eternaldays/.claude/get-shit-done/workflows/execute-plan.md
@/home/eternaldays/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/connector-sdk/src/index.ts
@connectors/github/src/index.ts
@connectors/slack/src/index.ts
@connectors/stripe/src/index.ts
@connectors/discord/src/index.ts
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nextra v4 docs site scaffold</name>
  <files>
    apps/docs/package.json
    apps/docs/next.config.mjs
    apps/docs/tsconfig.json
    apps/docs/app/layout.tsx
    apps/docs/app/page.mdx
    apps/docs/app/docs/_meta.ts
    apps/docs/mdx-components.tsx
  </files>
  <action>
    1. Create `apps/docs/` directory structure

    2. Create `apps/docs/package.json`:
       - name: "@feelr/docs"
       - private: true
       - scripts: { dev: "next dev", build: "next build" }
       - dependencies: next (match dashboard version ~15.5), react, react-dom, nextra, nextra-theme-docs
       - devDependencies: typescript, @types/react, @types/node

    3. Create `apps/docs/next.config.mjs`:
       - Import nextra from 'nextra'
       - Create withNextra wrapper
       - Export withNextra config with `output: 'export'` and `images: { unoptimized: true }` for static hosting
       - Use App Router (Nextra v4 requirement per research pitfall 5)

    4. Create `apps/docs/tsconfig.json`:
       - Extend from a base config or define minimal config with jsx: "preserve", moduleResolution: "bundler", paths for @/*

    5. Create `apps/docs/mdx-components.tsx`:
       - Export useMDXComponents from nextra-theme-docs (required by Nextra v4)

    6. Create `apps/docs/app/layout.tsx`:
       - Root layout importing nextra-theme-docs theme
       - Site title "Feelr Docs", description "Agent-friendly API simplification layer"
       - Configure sidebar navigation with docs structure
       - Include link to GitHub repo and self-hosting docs in repo

    7. Create `apps/docs/app/page.mdx`:
       - Landing page with hero: "Feelr Documentation"
       - Brief description of what Feelr does (one line per context decision: developer casual tone)
       - Cards/links to: Quick Start (API), Quick Start (CLI), Connector Reference, Self-Hosting

    8. Create `apps/docs/app/docs/_meta.ts`:
       - Nextra v4 navigation structure defining sidebar order:
         - getting-started-api: "Quick Start (API)"
         - getting-started-cli: "Quick Start (CLI)"
         - auth: "Authentication"
         - connectors: "Connectors"
         - cli-reference: "CLI Reference"
         - self-hosting (external link to GitHub repo self-host/ directory)

    9. Run `cd /home/eternaldays/claudeRepos/feelr && pnpm install` to register the workspace
  </action>
  <verify>
    `cd /home/eternaldays/claudeRepos/feelr && pnpm turbo typecheck --filter=@feelr/docs` passes (or at minimum `pnpm install` succeeds and the package is recognized in the workspace).
  </verify>
  <done>
    Nextra v4 docs site scaffold exists at apps/docs/ with App Router layout, static export config, navigation structure, and landing page. Package is registered in pnpm workspace.
  </done>
</task>

<task type="auto">
  <name>Task 2: Quick-start guides, auth setup, and connector references</name>
  <files>
    apps/docs/app/docs/getting-started-api/page.mdx
    apps/docs/app/docs/getting-started-cli/page.mdx
    apps/docs/app/docs/auth/setup/page.mdx
    apps/docs/app/docs/connectors/github/page.mdx
    apps/docs/app/docs/connectors/slack/page.mdx
    apps/docs/app/docs/connectors/stripe/page.mdx
    apps/docs/app/docs/connectors/discord/page.mdx
    apps/docs/app/docs/cli-reference/page.mdx
  </files>
  <action>
    Use developer casual tone (like Stripe docs -- clear, concise, code-heavy, friendly). Per CONTEXT.md decision: tone is Claude's discretion. Developer casual fits agent-focused dev tools best.

    1. Create `apps/docs/app/docs/getting-started-api/page.mdx` (API-first quick start):
       - Title: "Quick Start: API"
       - Steps: Get an API key (link to dashboard or admin API) -> Store a credential -> Make your first request
       - Show curl examples for each step:
         - `curl -X POST api.feelr.dev/admin/keys -H "Authorization: Bearer $ADMIN_TOKEN"`
         - `curl api.feelr.dev/v1/github/repos.list -H "X-Feelr-Key: fk_live_..."`
       - Show the response envelope format
       - Link to connector reference for more actions

    2. Create `apps/docs/app/docs/getting-started-cli/page.mdx` (CLI-first quick start):
       - Title: "Quick Start: CLI"
       - Steps: Install (show all 4 methods equally: Homebrew, curl, go install, GitHub Releases per decision) -> `feelr init` -> `feelr auth github` -> `feelr run github repos.list`
       - Show terminal output examples
       - Mention `feelr tools` for discovery
       - Link to CLI reference for all commands

    3. Create `apps/docs/app/docs/auth/setup/page.mdx`:
       - Title: "Authentication"
       - Explain the two auth layers: Feelr API key (X-Feelr-Key) and upstream credentials
       - API key section: how to create, manage, tiers (free/pro/team)
       - Credential section: how to store PATs (GitHub) and use OAuth (Slack, Discord)
       - Security notes: encrypted at rest, refresh coordination

    4. Create connector reference pages. For each connector (github, slack, stripe, discord), create a page at `apps/docs/app/docs/connectors/{name}/page.mdx`:
       - Title: "{Name} Connector"
       - Auth type and setup instructions
       - Action reference table: Name | Description | Required Params | Returns
       - Read action definitions from the actual connector source files to generate accurate tables
       - Include 2-3 example curl commands for the most common actions per connector
       - Per CONTEXT.md: hybrid approach -- schema tables from ActionDefinition types + hand-written examples for common actions

       GitHub actions (from 03-02 SUMMARY): repos.list, issues.list, issues.create, issues.update, issues.close, pulls.list, pulls.create, pulls.merge, issues.comment, repo.get
       Slack actions (from 05-01): messages.send, channels.list, channels.info, messages.history, search.messages, users.list
       Stripe actions (from 05-02): customers.list, customers.create, charges.list, charges.create, invoices.list, invoices.create, payments.list, products.list
       Discord actions (from 05-03): messages.send, messages.list, channels.list, channels.info, roles.list, roles.assign, members.list

    5. Create `apps/docs/app/docs/cli-reference/page.mdx`:
       - Title: "CLI Reference"
       - Commands: run, tools, status, auth, init, chain (run/list/validate/show)
       - Flags: --format, --verbose, --dry-run, --cursor, --profile, --search
       - Exit codes: 0 success, 1 error, 2 auth required, 3 not found, 4 usage error
       - Configuration: config file location, profile support, env var override

    6. Add `_meta.ts` files in subdirectories (auth/, connectors/) for sidebar ordering if needed by Nextra v4
  </action>
  <verify>
    All MDX files exist. Quick-start guides cover both API and CLI paths. All 4 connector reference pages have action tables. Self-hosting content is NOT duplicated (just linked).
  </verify>
  <done>
    Documentation site has complete content: 2 quick-start guides (API + CLI), auth setup page, 4 connector reference pages with action tables and examples, CLI reference, and external link to self-hosting docs in repo.
  </done>
</task>

</tasks>

<verification>
- `apps/docs/` directory exists as a valid pnpm workspace package
- `pnpm install` succeeds from monorepo root
- All MDX content files exist for: landing, 2 quick-starts, auth, 4 connectors, CLI reference
- Nextra config uses App Router (not Pages Router)
- Static export enabled (output: 'export')
- Self-hosting docs linked to repo, not duplicated
- Docs tone is developer casual (code-heavy, clear, friendly)
</verification>

<success_criteria>
Documentation site at apps/docs is a complete Nextra v4 project with static export capability, containing quick-start guides for both API and CLI users, authentication setup instructions, per-connector action reference tables with examples, and CLI reference. Self-hosting docs are linked to the repo, not duplicated.
</success_criteria>

<output>
After completion, create `.planning/phases/10-launch-prep/10-02-SUMMARY.md`
</output>
