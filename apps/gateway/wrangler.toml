# Feelr Gateway - Multi-Environment Configuration
#
# IMPORTANT: Do NOT run `wrangler deploy` without --env.
# Always use: `wrangler deploy --env staging` or `wrangler deploy --env production`
#
# Worker Secrets (set per-env via: npx wrangler secret put <NAME> --env <ENV>)
# Required: ENCRYPTION_KEY, ADMIN_TOKEN
# For Slack OAuth: SLACK_CLIENT_ID, SLACK_CLIENT_SECRET

name = "feelr-gateway"
main = "src/index.ts"
compatibility_date = "2026-02-05"

# keep_vars preserves dashboard-configured vars on deploy
keep_vars = true

# DO migrations are top-level -- inherited by all environments
[[migrations]]
tag = "v1"
new_sqlite_classes = ["TokenCoordinator"]

# Cron triggers -- daily retention cleanup at 3am UTC
[triggers]
crons = ["0 3 * * *"]

# ============================================================
# STAGING ENVIRONMENT
# Deploy: npx wrangler deploy --env staging
# Access: https://feelr-gateway-staging.<subdomain>.workers.dev
# ============================================================
[env.staging]
workers_dev = true  # Enable .workers.dev URL for staging access

[env.staging.vars]
ENVIRONMENT = "staging"

[[env.staging.kv_namespaces]]
binding = "AUTH_KV"
id = "PLACEHOLDER_STAGING_KV"

[[env.staging.d1_databases]]
binding = "USAGE_DB"
database_name = "feelr-usage-staging"
database_id = "PLACEHOLDER_STAGING_D1"

[env.staging.durable_objects]
bindings = [
  { name = "TOKEN_COORDINATOR", class_name = "TokenCoordinator" }
]

[[env.staging.unsafe.bindings]]
name = "RATE_LIMIT_FREE"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 30, period = 60 }

[[env.staging.unsafe.bindings]]
name = "RATE_LIMIT_PRO"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 300, period = 60 }

[[env.staging.unsafe.bindings]]
name = "RATE_LIMIT_ENTERPRISE"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 3000, period = 60 }

[[env.staging.unsafe.bindings]]
name = "RATE_LIMIT_IP"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 100, period = 10 }

# ============================================================
# PRODUCTION ENVIRONMENT
# Deploy: npx wrangler deploy --env production
# Access: https://api.feelr.dev
# ============================================================
[env.production]
workers_dev = false  # Disable .workers.dev URL -- production uses custom domain only

[[env.production.routes]]
pattern = "api.feelr.dev"
custom_domain = true

[env.production.vars]
ENVIRONMENT = "production"

[[env.production.kv_namespaces]]
binding = "AUTH_KV"
id = "PLACEHOLDER_PRODUCTION_KV"

[[env.production.d1_databases]]
binding = "USAGE_DB"
database_name = "feelr-usage-production"
database_id = "PLACEHOLDER_PRODUCTION_D1"

[env.production.durable_objects]
bindings = [
  { name = "TOKEN_COORDINATOR", class_name = "TokenCoordinator" }
]

[[env.production.unsafe.bindings]]
name = "RATE_LIMIT_FREE"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 30, period = 60 }

[[env.production.unsafe.bindings]]
name = "RATE_LIMIT_PRO"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 300, period = 60 }

[[env.production.unsafe.bindings]]
name = "RATE_LIMIT_ENTERPRISE"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 3000, period = 60 }

[[env.production.unsafe.bindings]]
name = "RATE_LIMIT_IP"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 100, period = 10 }
